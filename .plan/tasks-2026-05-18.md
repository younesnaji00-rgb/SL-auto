# Batch 2026-05-18 — ATG location/missions + chiffrage + dossier UX (loop-driven)

## Work request

> add an option to add the location of the agent de terrain if hes unavailable
> make the "address complete row: accept location links
> show the actual location of the agent de terrain when creating a mission and show when was it last updated
>
> when the user clicks on the start button in assignations agent de terrain, it fails to take into account their live location as their departure point, its never included in their path in google maps
>
> cloud messaging for each new mission in assignations agent de terrain, meaning whenever theres a new mission for the specific account then they shall see the mission through their notifications, they shall as well see the location request notification, the app will always take their live location as long as the account is logged in their app, even if the app is unopened,
>
> in gestion des dossiers, in the exporter button, rename it to "rappeler" it shall still have the same selection function, but instead of getting saved as an excel format, it shall work as a notification where it will remind the specified user to rework on the rows selected, so after the specific rows are selected, there should be an option called "envoyer a" and thats where a list of all the gestionnaires shall pop up, the user will be allowed to send to one or more gestionnaires, and one the gestionnaires are selected, the list shall pop up in the "mes rappels" button where it shall show them the selected rows in the same table format when the gestionnaire will click on each row and have the same workflow, the "mes rappels button" will work as a notification button where it shall show how many rows are inside that page as a notification, basically showing a "+n" as a bordered notification,
>
> in assignations chiffrage, rename the ref column into "reparation/remplacement"
>
> when the user clicks on sauvegarder, the three columns that include "accord or proposition" should only show total HT accorde or total TTC accorde, meaning it will only become one additional column, but the web editor shall be preserved, im only talking about the final pdf after the user clicks on sauvegarder, but since its only one column but there are two options which is ttc or ht, they both shall vary depending on wether the tva checkbox is checked or not, it shall be explained after this, meaning the total HT or TTC are interchangeable depending on wether the sans tva box in checked or not
>
> in the web editor add a check box called "sans TVA" when its checked, it will it will calculate in HT, meaning the tva percentage will be not calculated in the total, when when its not checked it shall calculate in TTC meaning it takes into account the TVA percentage, so the final column in the web editor shall be called "Prix Total Accordé" to not have conflicts with the other columns, but this is only for the web editor
>
> in gestion des dossiers, when i click on the eye icon to see the document to be scanned by the ai in creation de mission, i should be able to zoom in when i double click and also drag and hover to move the image exactly like how it works in WhatsApp web when i click on an image
> when i delete this image i should be able to import another one and redo the scan
>
> in information assure in creation de mission, unify the nom and prenom fields into nom complet, meaning the whole name will be written in one field
>
> in vehicule section in information assure in creation de mission, the immatriculation field should be from the carte grise card slot thats in piece jointes, meaning there should be an ai scan that scans where the immatriculation is,
>
> disable the import function in rapport final, the rapport final card slot will only populate after the user clicks on generer le rapport, the rapport generated will go towards rapport final card slot, there are multiple rapport types, so there should be a row for each rapport type, dont make it hard coded, since i will add new rapport types in the future
>
> when i click on a dossier and go to the planification tab, i dont see any mission, the table is empty, and it prompts me to create a new mission, but when i collapse the planification section and protract it again, i see active planifications, the issues is that the active missions dont populate automatically, its ground for a lot of confusion when its pushed to production
>
> make the valider le traitement button only available after the user imports a screenshot as proof in attacher une preuve button
>
> the date de creation column should be moved right next to reference compagnie from the right
> the horizontal scroll bar shouldnt be at the bottom of the table in gestion des dossiers, it should always be visible throughout the scroll
>
> if the scanned carte grise has letters in arabic, they shall be translated to french, and heres to what it should be translated:
>  follows:
>     أ = A
>     ب = B
>     ت = T
>     ث = TH
>     ج = J
>     ح = H
>     خ = KH
>     د = D
>     ذ = Z
>     ر = R
>     ز = Z
>     س = S
>     ش = SH
>     ص = S
>     ض = D
>     ط = T
>     ظ = Z
>     ع = A
>     غ = GH
>     ف = F
>     ق = Q
>     ك = K
>     ل = L
>     م = M
>     ن = N
>     ه = H
>     و = W
>     ي = Y
>
> remove the pen icon in gestion des dossiers in the table
>
> in the status and observations column, when i click on the bordered observation or status rows to check the history of observations and status through the table, nothing shows even though clearly there were other versions before it
>
> in the table in the matricule anterieure in gestion des dossiers, the cells never populate from the carte grise even though the carte grise clearly shows it, the carte grise is in the carte grise card slot, and i seen one case where it was populated and it was false since it picked the same matricule as the matricule definitive, it should pick from matricule anterieure instead of matricule, and its clearly written in the carte grise in its respective slot
>
> in assignations chiffrage in the table, add a new column for nom d'assure, and another one for immatriculation,
> inside the row in observation, when they click on it, it should show all the observations at once popping from the sidebar from the right
>
> i assigned a stamp to the chiffreur account in tampons page but i couldnt see it when i clicked on sauvegarder in assignations chiffrage in the web editor when i tried to add a stamp
>
> remove the consultation page from the chiffreur role
>
> in assignations chiffrage page, in the documents browser outside of the web editor, add the photos avant, en cours and apres options in the filter and remove these options: avis de dommage, bon de commande, cin/ identite, ordre de mission, permis de conduire, photos apres expertise, photos au moment du sinistre, photos avant expertise, procuration, pv de constat, rapport d'expertise
>
> i noticed that the devis and facture arent populate, those shall populate from devis garage and facture garage
>
> the observation section should be at the top and not at the bottom
>
> in tableau de bord, the date filters shouldnt default to today, it should default to tall time, and three preconfigured buttons for: aujourd'hui, semaine, mois
>
> remove the nouveau filter and replace it with creation de mission
>
> in assignations chiffrage, add the same multiple tabs function as in gestion des dossiers where each clicked row shall show in its own tab
>
> in assignations agent de terrain, add a search bar where the user may search for each mission through a keywork
>
> in each agent de terrain mission, add the numero d'immatriculation (definitive) in each mission card

## Global context

- Stack: Next.js 15.5.9 + React 19 + Firestore + Firebase Auth + Genkit AI flows + Tailwind/shadcn.
- Inner repo (app source): `c:/Users/pc/Downloads/SL-auto-main/SL-auto-main/` on `auto-2026-05-18` (created this firing from `main`).
- Outer repo: `c:/Users/pc/Downloads/SL-auto-main/` on `auto-2026-04-30`.
- Verify: `cd SL-auto-main && npm run typecheck`.
- Commit discipline: inner first, optional outer pointer bump only when a user-visible behavior change lands; never push; `git add <specific-paths>`, never `-A`.
- Pre-existing uncommitted edits on inner: `.gitignore`, `src/app/(app)/assignations-atg/page.tsx`. Untouched by loop — implementer must use `git add <specific paths>` only.

## Locked assumptions

- **A1 — sequencing**: user picked "Infrastructure first". FCM cloud messaging + GPS-driven location work come before quick UI wins.
- **A2 — FCM scaffold already exists**: `public/firebase-messaging-sw.js`, `src/firebase/messaging.ts`, `src/hooks/use-fcm-registration.ts`, `functions/src/index.ts` (Cloud Function `sendLocationRequestNotification` on `location_requests/{requestId}`), `src/hooks/use-gps-publisher.ts`, `src/components/gps-publisher-host.tsx`, PWA manifest — all landed in commit `0a0e0d3` on `main`. VAPID/Maps keys wired in `apphosting.yaml` (`6f26630`). My job is to **extend** this infra, not rebuild it.
- **A3 — FCM credentials**: VAPID is already wired via App Hosting env (`apphosting.yaml`). `NEXT_PUBLIC_FIREBASE_VAPID_KEY` is the env var name. Nothing to scaffold on the key side.
- **A4 — location model**: foreground GPS publisher (`useGpsPublisher`) exists; "Demander la localisation" flow works through the existing `location_requests`→FCM bridge. The "always-on even when app closed" part is fundamentally limited by browser PWA semantics (SW cannot read geolocation directly); we deliver the achievable subset and flag the limitation in follow-ups.
- **A5 — agent UID resolution**: planifications today store only `agentTerrain` (display name). The UID is derived at read time via `users where nom == name limit 1` (`use-agent-live-location.ts`). Server-side FCM dispatch needs the UID at write time, so storing `agentTerrainUid` on the planification doc is the unblocker for the next-mission notification Cloud Function.
- **A6 — branch & commit pattern**: matches the prior batch — inner `auto-2026-05-18`, outer `auto-2026-04-30`, never push.
- **A7 — loop discipline**: no queue; each firing picks exactly one next task; follow-ups go into `## Follow-ups`, never promoted to tasks.

## Completed

- [x] **planification-store-agent-uid** — Resolved agent UID now persisted on each new/updated planification doc as `agentTerrainUid` (null when no match). Mirrors the `users where nom == name limit 1` lookup from `use-agent-live-location.ts`, runs once above the create/update split, never blocks the save (try/catch warn). Extended the existing `firebase/firestore` import with `getDocs, query, where, limit` — no new import line. Unblocks the next-mission FCM Cloud Function task — inner `1523004` on `auto-2026-05-18`. typecheck clean. No outer bump. (reviewed ✓)
- [x] **cloud-fn-new-mission-notification** — Added Cloud Function `sendNewMissionNotification` (firebase-functions v2 `onDocumentCreated` on `dossiers/{dossierId}/planifications/{pid}`, region europe-west1) that consumes the `agentTerrainUid` field, fetches `users/{uid}.fcmTokens`, and dispatches multicast FCM titled "Nouvelle mission" with derived body. Mirrors the existing `sendLocationRequestNotification` shape: same `webpush` icon/badge, same `INVALID_TOKEN_ERRORS` cleanup, string-only `data` payload. Functions-level `npx tsc --noEmit` clean + app-level `npm run typecheck` clean — inner `c653427` on `auto-2026-05-18`. No outer bump. (reviewed ✓)
- [x] **address-input-parse-location-links** — "Adresse complète" `<Input>` in the planification modal now intercepts paste, parses Google Maps URLs (`@lat,lng`, `?q=lat,lng`, `?ll=lat,lng`, `?destination=lat,lng`), replaces the paste with `"<lat>,<lng>"` at 6-decimal precision. Unparseable strings paste naturally. Typed input untouched. Pure helper `parseMapsLinkOrSelf` colocated with `normalizeTypeMission`; no new imports, no deps — inner `4c43501` on `auto-2026-05-18`. typecheck clean. No outer bump. (reviewed ✓)
- [x] **show-agent-live-location-in-modal** — Added a positive info Alert immediately before the existing stale-position alert in the planification modal. Renders when `formData.agentTerrain && agentLive.isFresh && agentLive.location`: title "Position actuelle de l'agent", monospace `lat, lng` to 5 decimals, italic relative-time stamp via `formatDistanceToNow(..., { locale: fr, addSuffix: true })`, and a "Voir sur Google Maps" link. `formatDistanceToNow` added to existing `date-fns` import inline — inner `140c60b` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **scan-transliterate-arabic-matricule** — Module-scoped `ARABIC_TO_LATIN` (28 entries, user spec) + pure `transliterateArabic(s)` helper, applied to `data.registration` and `data.previousRegistration` just before `return NextResponse.json(...)` — inner `ac8df6b` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **rbac-remove-consultation-from-chiffreur** — Removed `'Chiffreur'` from BOTH the sidebar nav role list AND the page-level guard `CONSULTATION_ALLOWED_ROLES`. Preserved chiffreur access to `/assignations-chiffrage`. 2 files, +1/-2 — inner `24527e9` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **dossiers-remove-pen-icon** — Deleted the "Éditer web" Button block, the `canEditWeb` gate + comment, and the `Pencil` token from the lucide-react import. Adjacent Eye/History row actions intact. +1/-55 — inner `665068e` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **devis-editor-rename-ref-column** — REF → Réparation/Remplacement column header in the chiffrage editor (3 string edits, widened from 90px → 180px). REF_OPTIONS / r.ref / PDF code paths untouched — inner `70971ca` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **dossiers-rename-nouveau-to-creation-de-mission** — Both new-dossier buttons in gestion des dossiers ("Créer un dossier" top + "Nouveau dossier" empty state) → "Création de mission". Status fallback `'Nouveau'` correctly preserved. +2/-2 — inner `3d840a2` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **dashboard-date-filter-defaults-and-presets** — Date filter defaults to ALL TIME; 3 preset buttons (Aujourd'hui / Semaine / Mois) above Du/Au pickers; counter sentence renders "au total" when both dates undefined. +51/-8 — inner `365bba1` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **dossiers-move-date-creation-next-to-ref-compagnie** — Date de création column moved to immediately after Référence de compagnie. EXPORT_COLUMNS + body row swapped in lockstep. +2/-2 — inner `f017fda` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **dossiers-sticky-table-scrollbar** — Card bounded via `max-h-[calc(100vh-280px)] overflow-auto`; TableHeader pinned with `sticky top-0 z-10 bg-card`. +2/-2 — inner `18f007a` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **assignations-chiffrage-add-assure-matricule-columns** — Nom d'assuré + Immatriculation columns added between Dossier and Chiffreur. Two new state maps populated in the existing per-dossier onSnapshot; `colCount` bumped 8/7 → 10/9. +15/-1 — inner `2297ad3` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **information-assure-unify-nom-prenom** — Information-tab assuré Nom + Prénom collapsed into single "Nom complet" input. Hydration folds legacy `assure.prenom` into `assure.nom`. +3/-3 — inner `da4ceaf` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **slot-card-disable-rapport-import** — Manual rapport import disabled via extension of existing `hideUploadForAccord` gate; scales to future "Rapport X" types via prefix detection. +4/-2 — inner `46e185f` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **document-types-curate-list** — Replaced global `DOCUMENT_TYPES` with curated 8-entry list. +4/-12 — inner `3a95b73` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **observations-valider-require-proof** — Per-observation "Valider le traitement" Button now gated on `proofs.length > 0`; tooltip added when disabled. +2/-1 — inner `8fe3695` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **chiffrage-detail-move-observations-to-top** — `<ObservationsTab>` moved from page bottom to immediately after the header. +3/-3 — inner `172f272` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **devis-editor-sans-tva-toggle** — Sans-TVA checkbox + accord-triple third column renamed and computes HT/TTC dynamically. +17/-3 — inner `6cea7f5` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **planification-tab-skip-empty-cache-snapshot** — Guard added to skip stale empty cache snapshots. +6/-0 — inner `f5ef2a4` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **stamps-chiffreur-shows-assigned-not-created** — Switched `useStamps({mineOnly})` filter from `createdBy` to `assignedStampIds`. +9/-2 — inner `33c5ca7` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **status-history-sheet-no-composite-index** — Dropped `orderBy` from the query, sort moved to client-side `sortedEntries` useMemo with a robust `tsOf` helper. +16/-4 — inner `ead0fa7` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **scan-carte-grise-transliterate-arabic** — Module-scoped `ARABIC_TO_LATIN` (28 entries) + `transliterateArabic` helper copied from scan-document/route.ts; applied to reg/prev post-trim. +39/-2 — inner `7acc049` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **carte-grise-scan-also-write-vehicule-immatriculation** — Carte-grise scan now writes to both `matricule` and `vehicule.immatriculation`. +4/-1 — inner `3e89268` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **documents-filter-rollup-devis-facture-garage** — `Devis *` docs roll into the "Devis" filter row; same for Facture. Drill-down family unchanged. +11/-1 — inner `e4ba99d` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **observation-history-sheet** — Observation TableCell now opens an amber-rail history sheet querying `dossiers/{id}/observations`. +109/-1 — inner `daf72dd` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **lightbox-zoom-pan-pinch** — WhatsApp-style zoom/pan on image previews via react-zoom-pan-pinch. +15/-2 — inner `4e748da` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **mes-rappels-nav-and-empty-page** — `/mes-rappels` route + sidebar nav entry (Bell icon, roles Admin / Responsable d'équipe / Gestionnaire). +22/-0 — inner `b11c07d` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **mes-rappels-data-hook-and-table** — `useRappels` hook + Mes rappels page renders the recipient's rappels in a clickable table. +131/-6 — inner `425dd27` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **mes-rappels-sender-side-rappeler-button** — Rappeler/Envoyer-à dialog flow + multi-gestionnaire write into `rappels`. +102/-7 — inner `deb138f` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **mes-rappels-nav-badge** — `+N` bordered badge on sidebar `/mes-rappels` entry when unread > 0. +9/-0 — inner `506a9a3` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **mes-rappels-mark-read-on-click** — Row click on the Mes rappels page now fires a fire-and-forget `updateDoc(rappels/{id}, { read: true })` BEFORE the `router.push('/dossiers/${id}')`. Sidebar `+N` decrements as rappels are viewed. +9/-1 — inner `a6d2e75` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **assignations-chiffrage-observation-history-popover** — Reused iter-26 ObservationHistorySheet on the chiffrage listing. +15/-1 — inner `c6cd894` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **shared-transliterate-arabic-utility** — `ARABIC_TO_LATIN` + `transliterateArabic` extracted to `src/lib/transliterate-arabic.ts`; both scan routes now import. +38/-74 — inner `a025ccc` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **step-1-import-lightbox-with-delete** — Lightbox `onDelete` prop + step-1-import migrated to shared lightbox (zoom + delete). +23/-24 — inner `8731daa` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **documents-filter-blocklist-legacy** — User pushback: iter 16's constant-curate didn't actually hide the legacy labels because `DocumentsFilterPanel` unions observed types from docs. Added a module-scoped `REMOVED_FILTER_DOC_TYPES: ReadonlySet<string>` (the 11 user-listed labels) and applied it in BOTH `filterRows` (`.filter(label => !REMOVED.has(label))` between `Array.from(allLabels)` and `.map(...)`) and `filterGroups` (skip at top of the for-of loop body). `visibleDocs` untouched — docs themselves stay accessible under "Tous". 1 file, +20/-1 — inner `fbf704e` on `auto-2026-05-18`. typecheck clean. (reviewed ✓)
- [x] **devis-pdf-collapse-accord-triple-on-save** — `renderDevisPdf` now accepts `collapseAccordToTotal` + `sansTva` options. When set, each accord/proposition extra renders as a single "Prix Total Accordé/Proposé" column (HT when sansTva=true, TTC otherwise) instead of the legacy 3-column triple (PU / Total HT / Prix TTC). `DevisPreviewDialog` accepts `sansTva` prop and threads it through both `renderDevisPdf` calls (preview + confirm-with-stamp). `devis-editor.tsx` passes its `sansTva` state into the preview dialog. The save-time PDF now matches the editor's "Prix Total Accordé/Proposé" column exactly. Legacy renderer path (send-to-chiffrage.ts) unchanged. 3 files, +53/-13 — inner `7357ae8`. typecheck clean.
- [x] **chiffrage-multi-tabs-gestion-style** — New `useChiffrageTabs` hook + `ChiffrageTabsBar` component, parallel to the existing dossier tabs pattern (sessionStorage key `sl-auto:chiffrage-tabs`, route prefix `/assignations-chiffrage/`). Provider mounted in `(app)/layout.tsx` inside the existing DossierTabsProvider; bar rendered when the pathname matches `/assignations-chiffrage*`. Row Link in `assignations-chiffrage/page.tsx` registers a tab on click; detail page calls `openTab` + `refreshTabLabel` on snapshot so direct deep links also create a tab and the label tracks the dossier ref. 5 files, +281/-2 — inner `475d256`. typecheck clean.
- [x] **rapport-final-dynamic-per-type** — `typed-documents-grid.tsx` no longer hardcodes `RAPPORT_LABELS = new Set(['Rapport final'])`. Replaced with `isRapportLabel(s)` predicate (`s === 'Rapport final' || s.startsWith('Rapport ')`), and after building from `BASE_DOC_SLOTS`, the rapport list is unioned with any observed `Rapport *` types on live docs. Each rapport variant gets its own slot card in the "Rapport" section grid. 1 file, +22/-4 — inner `f07ec60`. typecheck clean.
- [x] **atg-live-coords-search-immat** — Three ATG features in one commit: (a) `openRouteForItems` reads `navigator.geolocation.getCurrentPosition` (4s timeout, falls back to "My+Location") and passes the resulting "lat,lng" as explicit `origin` in the Maps URL — solves the user complaint that Maps wasn't picking up live position; (b) keyword filter added (mobile Sheet + desktop bar) that matches across dossier ref / assuré / phone / adresse / zone / observation / compagnie / agent / type mission / matricule; (c) live `matricule` field added to `DossierLive` snapshot subscription and rendered as a `font-mono tabular-nums` line in each mission card. User's pre-existing WIP on the same file (3 small hunks) preserved via stash+pop. 1 file, +111/-12 — inner `5cecb72`. typecheck clean.

## Current

_(empty — fresh batch of 4 deferred items landed in this session: PDF accord collapse, chiffrage multi-tabs, dynamic rapport slots, ATG triple. Next ask from user picks the next target.)_


## Follow-ups

- **Pre-existing dirty files** (`.gitignore`, `src/app/(app)/assignations-atg/page.tsx`, `src/app/(app)/dossiers/[id]/planification-tab.tsx`) on inner `auto-2026-05-18` — not from the loop. ATG file is now clean again after the stash+pop dance around `5cecb72`. User edits restored verbatim. Loop never stages these unless user explicitly asks.
- **Short Google Maps URLs** (`goo.gl/maps/...`, `maps.app.goo.gl/...`) are NOT resolved by `parseMapsLinkOrSelf` — they contain no embedded coords, so the field accepts the raw short URL. Resolving them requires either a Cloud Function follow-redirect endpoint or client-side `fetch` (CORS-blocked from `goo.gl`). Defer until the user reports this is actually being pasted.
- **Unused firestore imports in `dossiers/client-page.tsx`** — after the pen-icon removal, `addDoc`, `collection`, `doc`, `updateDoc`, and `serverTimestamp` from `firebase/firestore` are no longer referenced in this file. Out of scope for the pen-icon task; TS does not flag them under the current tsconfig. Clean up only if a future task touches this file's imports — or in a dedicated cleanup pass.
- **Sticky-scrollbar magic number** — the `max-h-[calc(100vh-280px)]` on the Card assumes ~280px of chrome (page header + action bar + pagination). If future tasks add/remove rows of UI above or below the table, re-tune this offset. Easy fix when it surfaces.
- **Legacy doc-types still on existing Firestore docs** — after `[constants] curate-document-types-list`, existing documents tagged with `Rapport d'expertise`, `PV de constat`, `CIN/Identité`, etc. still surface in the filter via the observed-types path. Acceptable edge case for now. If user wants those filtered out entirely, add a blocklist in `documents-filter-panel.tsx` or run a data migration to reclassify.
- ~~**`sansTva` ephemeral / PDF mismatch**~~ — PDF parity landed in `7357ae8` (`collapse-accord-triple-on-save`). Sans-TVA remains ephemeral (resets per editor mount); add persistence only if the user later asks for the toggle to "stick" across sessions.
- **Empty-cache snapshot flash pattern** — the same first-empty-cache pattern that bit `planification-tab.tsx` likely affects other `onSnapshot` listeners in this codebase (documents tab, observations, dossier listing, etc.). Worth a defensive sweep if more "appears empty on first visit, then populates after re-mount" reports come in.
- ~~**Observation history popover**~~ — landed in iteration 26 (`daf72dd`).
- ~~**Lightbox "delete + redo scan" half**~~ — landed in iteration 35 (`8731daa`).
- **Excel-export dead code** — after `[mes-rappels] sender-side-rappeler-button`, `handleExport`, `EXPORT_COLUMNS`, `selectedColumns`, and the per-column-checkbox UI in `dossiers/client-page.tsx` are reachable from no UI affordance. They cost ~150 lines + the `lib/export-excel.ts` dependency. Clean up once the user confirms the rappel flow is working end-to-end and they don't want to keep Excel export as a parallel feature.
- ~~**Mes rappels badge counter on nav entry**~~ — landed in iteration 31 (`506a9a3`).
- ~~**Mark rappel as read on row click**~~ — landed in iteration 32 (`a6d2e75`).
- **Other composite-index queries** — same `where + orderBy on different fields` pattern may exist in other surfaces (observations history, planification history, etc.). Defensive sweep recommended if more "history shows empty" reports surface.
- **Carte-grise AI extraction reliability for `previousRegistration`** — Transliteration was applied this iteration, but the user's underlying complaint is that the AI often fails to detect the matricule antérieure even when it's clearly visible. Prompt strengthening + box-detection cues could improve recall; deferred until the user confirms transliteration alone unblocks them.
- **Shared transliteration utility** — `ARABIC_TO_LATIN` + `transliterateArabic` is now duplicated in `scan-document/route.ts` and `scan-carte-grise/route.ts`. If a third scanner needs it, refactor to `src/lib/transliterate-arabic.ts` instead of triplicating.
- **Browser PWA limitation on "always-on" GPS**: a service worker cannot directly call `navigator.geolocation`. The realistic achievable subset is: (a) foreground publisher while a tab is open, (b) interactive notification on `location_request` that prompts the agent to tap → opens app → foreground publisher writes. True "even-app-closed" GPS requires a native wrapper (Capacitor/Cordova/RN). Flag for user before they expect 24/7 background tracking.
- **Outer pointer bump pending**: every loop commit lands only on inner `auto-2026-05-18`. Outer `auto-2026-04-30` needs a bump before Firebase Hosting can serve the new code.
