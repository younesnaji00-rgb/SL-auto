# Batch 2026-05-21 — 11-item polish (permissions, rappels-log, manual-loc, deadline, obs-perso, photos-by-loc, vetuste-col-cap, lightbox, chiffreur-attribution, accord2-routing, cree-par-col)

## Work request

> (item #1 from user's original 12 dropped on 2026-05-21 — feature already implemented in `src/app/(app)/assignations-atg/page.tsx:1011-1013`, confirmed by user as "i forgot my bad")
>
> add toggles for permissions for each role, this is in the utilisateurs page, basically when i click on each user row it takes me to a page where i can toggle their permissions, meaning what they can access, for now the options should be all the pages that are shown in the left panel aside from signaler un bug since it shall be accessible for every user
>
> add logs for mes rappels, meaning when a user sends a rappel to a gestionnaire, it shall keep a log of what they sent and to who in a table, it shall show how many dossiers, to who and when, and when i click on the table, it shall show me all the dossiers that were sent, with the same read receipts, but the read receipts are to be affected by the recipient and not the sender, meaning if the recipient clicks on the dossier and lands on the page thats where it will show that it was read
>
> should be able to manually specify the location of the agent de terrain through creation de planification in the gestion des dossiers page by enabling a new row in creation de planification in case the localisation of the agent de terrain was impossible to get
>
> the deadline counter doesnt show how late the agent de terrain is on the newly made missions
>
> in creation de planification add a new observation field for observation personalisee
>
> in photos in planification, aside from partitionning photos per date, add a feature to partition by location
>
> in the web editor, in one row, i increased the vetuste value to the limit which is 50 percent, but when i went to the column title and increased every vetuste row at once, the limit was surpassed
>
> attached preuves shouldnt take me to a new page when i click on them, everything shall be shown in a lightbox
>
> in the documents card slots in gestion des dossiers in pieces jointes and also in accord and accord 2 et +, show which chiffreur had done the chiffrage
>
> when i do modification in the page of assignations chiffrage in 2eme accord or 2eme proposition and click save, it doesnt show in 2eme accord et + section in gestion des dossiers page but instead show in accord section in gestion des dossiers, fix that
>
> add a column for cree par in gestion des dossiers in gestion des dossiers and create a filter specifically for it, such that we'll know who created a dossier.

## Global context

- Stack: Next.js 15.5.9 + React 19 + Firestore + Firebase Auth + Genkit AI flows + Tailwind/shadcn.
- Inner repo (app source): `c:/Users/pc/Downloads/SL-auto-main/SL-auto-main/` on `auto-2026-05-20` (head `bfbd6de`).
- Outer repo: `c:/Users/pc/Downloads/SL-auto-main/` on `auto-2026-05-20` (head `e097b02`). No outer bump this batch unless user asks.
- Verify (mandatory before any "done"): `npm run typecheck` (from inner cwd). Also `npm run lint` (matches a baseline that exits 1 on a pre-existing Next 16 ESLint deprecation — implementers compare diff, not exit code).
- Commit discipline: inner first, never push, `git add <specific paths>` only, never `-A`. Commit message format: `[<short-title>] <task title>`.
- Pre-existing dirty files on inner: `.plan/tasks.md` only (this file). No user WIP to preserve this batch.

## Locked assumptions

- **A1 — branch**: stay on inner `auto-2026-05-20`; do NOT create `auto-2026-05-21` since we're already on a non-main auto branch (per loop prompt rule).
- **A2 — verify command**: `npm run typecheck` is the hard gate (exit 0 required). `npm run lint` is best-effort; baseline exits 1 due to pre-existing Next 16 ESLint deprecation, so implementers compare diff signal, not exit code. `npm run build` only if a build-impacting change (e.g., new route, new env var).
- **A3 — item #2 (permissions)**: TBD. Needs clarification when reached — per-user vs per-role-with-overrides, storage model in Firestore (`users/{uid}.permissions` map vs `roles/{role}.permissions`), and whether sidebar items have stable IDs.
- **A4 — item #3 (rappels logs)**: TBD. Needs clarification when reached — read-receipt scope (per recipient-dossier pair?), where the log table is rendered (new page vs section in mes-rappels), whether the existing rappels collection has the necessary fields or needs schema extension.
- **A5 — item #11 (2eme accord routing bug)**: root-cause first via `systematic-debugging` skill. Likely a missing/wrong category flag on save in `assignations-chiffrage/[id]/page.tsx`. Do not patch symptom on the gestion-des-dossiers display side.
- **A6 — item #8 (vetuste column-level cap)**: same 50% cap must be enforced when the bulk handler updates rows. Likely a missing `Math.min(value, 50)` clamp in the column-header handler.
- **A7 — loop discipline**: no queue. This iteration covers context capture + first task only. Subsequent firings discover one task at a time.
- **A8 — Co-Authored-By trailer**: prior batches included a Co-Authored-By line on every commit. Implementers should continue this pattern unless the user instructs otherwise.

## Completed

- [x] **add-observation-personnalisee-field** — `095d3a1` (reviewed ✓). New `<Textarea>` labelled "Observation personnalisée" added to `modal-planification.tsx` immediately after the existing Observation Select and before feasibility alerts. `observationPersonnalisee: ''` added to `formData` initial state and both `useEffect` reset branches; rehydrated from `initialData.observationPersonnalisee` on reopen; included in shared Firestore `payload` consumed by both `updateDoc` and `addDoc`. Existing observation Select untouched. Payload typed `Record<string, any>` so no external schema needed. 1 file, +14/-1. typecheck clean.
- [x] **vetuste-column-bulk-respect-50-cap** — `11ad415` (reviewed ✓). Root cause: per-row clamp uses `CellNumberInput` `max={50}` (clamps on stepUp/stepDown/blur); the column-header bulk handlers in `devis-editor.tsx:1237-1253` bypass `CellNumberInput` entirely and used `Math.min(100, ...)`. Fix: two literal substitutions `Math.min(100, ...)` → `Math.min(50, ...)` in both the `-5` and `+5` bulk handlers (lines 1240 and 1250). Handler is incremental (±5), so repeated clicks saturate at 50 instead of climbing to 100. Per-row clamp unchanged. 1 file, +2/-2. typecheck clean.
- [x] **preuves-lightbox-no-navigation** — `b77ffb4` (reviewed ✓). Discovery: the ATG dossier page already had a preuve lightbox (line 876) — only `observations-tab.tsx` (the observation-attached `traitementProofs`) was opening proofs via `<a target="_blank">`. Fix: replaced the anchor with a `<button>` that sets `lightboxProof` state; rendered a shadcn `<Dialog>` with `<img>` for images and `<iframe>` for PDFs (proofs accept `image/*` and `application/pdf`). shadcn Dialog provides ESC / overlay-click / X-close natively. Added a sr-only `DialogTitle` for Radix a11y. Thumbnail layout/sizing/hover preserved. 1 file, +41/-6. typecheck clean.
- [x] **chiffreur-attribution-on-doc-slots** — `442acba` (reviewed ✓). Discovery: chiffrage docs carry `uploadedByName` (string) and `uploadePar` (email); the chiffreur path in `devis-editor.tsx:830-841` writes only those (no `uploadedBy` uid), while the gestionnaire path in `send-to-chiffrage.ts:340-342` writes all three. No dedicated `chiffreurUid` field anywhere. Implementer correctly diverged from the suggested file (`slot-card.tsx` is where slot docs actually render, not `typed-documents-grid.tsx`) and from the suggested helper (`UserNameLink` leaks "Utilisateur inconnu" on missing data, violating done criterion #5 — used raw `uploadedByName` instead). Render: small muted `text-[10px] text-muted-foreground truncate` footer "Chiffré par {name}", gated by `parseAccordDocType(slot) != null` so non-chiffrage docs (carte grise, scans, rapports) skip the attribution. Empty `uploadedByName` renders nothing. Note: reviewer confirmed chiffrage docs always land in accord/proposition slots in practice — never raw "pieces jointes" — so the user's "pieces jointes" mention is conceptually included but inert here. 1 file, +17/-0. typecheck clean.
- [x] **manual-agent-location-fallback** — `6fb2a6d` (reviewed ✓). Discovery: "agent location unavailable" was an inline expression `formData.agentTerrain && !agentLive.isFresh && agentLive.agentUid` driving the existing alert. Implementer extracted it into a derived const `isAgentLocationUnavailable` so the alert, the new input row, and `handleSave` all share one predicate. New `agentLocationManuel: ''` added to `formData` (init, reset, edit-prefill branches); conditional `<Input>` labelled "Localisation de l'agent (manuelle)" renders only when unavailable, positioned directly below the unavailable Alert. Trimmed value persisted only when `isAgentLocationUnavailable` is true on save; cleared otherwise so stored values don't leak after GPS recovers. **Caveat**: downstream `useAtgFeasibility` needs lat/lng coords, not a free-form string — manual value is persisted but NOT yet consumed by feasibility (would require server-side geocoding). Logged as follow-up. 1 file, +37/-2. typecheck clean.
- [x] **accord2-routing-correct-section** — `e9e9acd` (reviewed ✓). Root cause (write-side per A5): `devis-editor.tsx` `handleSave` ordinal derivation compared `parsedAccordSlot.sourceDocType === docType`. For extra garages (`docType = 'Devis Garage 2'`), `parsedAccordSlot.sourceDocType` is always the canonical base `'Devis Garage'`, so equality failed and `else if` only accepts base docTypes — extras fell through to `ordinal = 1`. Result: 2ème accord/proposition saves on extra garages landed in the family's ordinal-1 slot, hiding them from `cardinalFilter='2-plus'` "accord 2 et +" section (step 6 uses default `cardinalFilter='all'` so the mis-saved doc showed up in "accord"). Fix: compare `parsedAccordSlot.parent === docType` so the URL slot is honored for base AND extras (parent === sourceDocType for base, so base case unchanged). Implementer correctly diverged from the suggested file — root cause was in the editor's save handler, not the page. 1 file, +12/-1. typecheck clean.
- [x] **deadline-shows-lateness-on-new-missions** — `6245648` (reviewed ✓). Root cause was hint (c): the `getDeadlineInfo` helper inline in `assignations-atg/page.tsx` anchored its pending→expired transition at 8am of the RDV day (`startTime.setHours(8, 0, 0, 0)`) and only flipped to "En retard" after the 24-business-hour SLA expired. A planification with RDV today 09:00 viewed at 11:00 displayed "21h restants" instead of any lateness indicator. Fix: split the anchor into `rdvMoment` (date+time, drives pending→expired) and `slaStart` (8am, drives `elapsedHours` for `formatBusinessLateness`). Once `now >= rdvMoment`, badge shows "En retard" immediately. Already-overdue planifs unchanged because `slaStart`/`elapsedHours` math preserved. `modal-planification.tsx` already writes `createdAt: serverTimestamp()` — hint (a) ruled out. `DeadlineBadge` is defined inline in `assignations-atg/page.tsx`, not in its own file. 1 file, +20/-12. typecheck clean.
- [x] **cree-par-column-and-filter** — `d39ca46` (reviewed ✓). Discovery: `create-empty-dossier.ts` already writes both `createdBy` (uid) and `createdByName` (displayName / email / `'Utilisateur'` fallback). `Dossier` type was missing `createdByName` — implementer added it. `UserNameLink` was a poor fit (expects different prop shape) so the column reads `createdByName` directly. Changes: added `createdByName` to `EXPORT_COLUMNS` rightmost after `dateRequete`; added `creator` filter (default `'Tous'`) integrated with existing AND-filter logic; column-header popover filter matching existing compagnie/observation pattern; distinct-creators list built from `allDossiers` per spec (not from system-wide user list); active-filter badge + reset-all clears it; body cell renders `—` for empty/legacy dossiers; bumped loading/empty `colSpan` 16 → 17. 2 files, +83/-5. typecheck clean.

- [x] **photos-partition-by-location-option** — `b688492` (reviewed ✓). Discovery: photos in `dossiers/{id}/photos` carry NO structured location metadata today; ATG uploads (via `photo-watermark.ts`) capture GPS but burn it into the image as a watermark only — no `lat`/`lng`/`location` field is persisted. Implementer chose a forward-compatible hybrid grouping `(a) + (c) + (d)`: prefer a `location` text field if present, else round `lat`/`lng` to 3 decimals (~100 m bucket), else fall into "Sans localisation". The toggle works structurally today (everything falls under "Sans localisation") and becomes immediately useful the moment uploads persist a `location` or `{lat, lng}` field. UI: 170-px `<Select>` next to upload button on each category tab (Par date / Par localisation). Shared `renderPhotoCard` closure between both modes so per-date behavior is byte-identical. 1 file, +332/-149. typecheck clean.

- [x] **per-user-permissions-restrictive-overrides** — `b721205` (reviewed ✓). Design (clarified with user this iter): Restrictive overrides — role gate is the baseline, per-user toggles can only DENY within the role-allowed set. "Signaler un bug" always visible. Storage: `users/{uid}.deniedNavItems: string[]` of nav `href` values. Implementation: 4 files. `use-current-user.tsx` — hydrates `deniedNavItems` from Firestore snapshot, coerces missing/non-array to `[]`. `layout/sidebar.tsx` (NOT `ui/sidebar.tsx` — that's the shadcn primitive; the layout file is the actual render site) — applies denial filter AFTER role gate with `/signaler-bug` short-circuited. `utilisateurs/client-page.tsx` — row click navigates to `/utilisateurs/[uid]` with `stopPropagation` on password/action cells so their controls still work. `utilisateurs/[uid]/page.tsx` — Permissions card, role-filtered toggle list using existing `isItemVisibleToRole` helper, optimistic UI with rollback on `updateDoc` failure, per-row "Enregistrement…" cue. Role gates in `nav-groups.ts` untouched. 4 files, +124/-5. typecheck clean.

- [x] **rappels-sent-log-with-recipient-side-read-receipts** — `60e0f5d` (reviewed ✓). Design (clarified with user this iter): Tabs on `/mes-rappels` (Reçus default / Envoyés new). Batching: `batchId` (UUID via `crypto.randomUUID()` with `Date.now()`/`Math.random()` fallback) added to all rappel docs in one send. Read trigger: recipient lands on dossier page (any entry point). Implementation: 4 files. `dossiers/client-page.tsx` — `handleSendRappel` generates one `batchId` shared across all `(recipient × dossier)` writes. `use-rappels.ts` — added `batchId?` field to `Rappel`; added `useRappelsSent()` mirroring `useRappels` with `senderUid` filter + stale-cache skip + DESC sort. `mes-rappels/page.tsx` — wrapped in shadcn `<Tabs>`; Reçus default (click-to-mark-read kept for UX redundancy, documented inline); Envoyés groups by `{batchId || solo:${id}, recipientUid}`, shows recipient/count/latest-date/"X / Y lus", expands inline per-dossier list with badges + `/dossiers/{id}` links. `dossiers/[id]/page.tsx` — one-shot `useEffect` keyed on `[db, id, profile?.uid]` queries unread rappels and commits `writeBatch` of `read: true`; idempotent + cancel-guarded. 4 files, +300/-56. typecheck clean.

## Current

_(empty — all 11 items shipped. Loop complete.)_

## Follow-ups

- **Planification TypeScript type** — if a `Planification` interface is ever introduced in `src/lib/` (currently the payload is typed `Record<string, any>`), the new `observationPersonnalisee` field should be added there. Out of scope this iter.
- **Read-side display of `observationPersonnalisee`** — any view that displays a planification doc may want to show the new field. Sweep when surfaced by user feedback; otherwise wait.
- **Vetuste 50% cap as magic number** — `50` appears in three places now (per-row `max={50}` at devis-editor.tsx:1517; bulk handlers at lines 1240, 1250). Worth extracting to a shared constant (e.g. `MAX_VETUSTE_PERCENT`). Out of scope this iter per task spec.
- **Vetuste schema range mismatch** — `src/lib/devis-schema.ts` comment says "0-100" for vetuste while UI enforces 0-50. Align the schema doc/refinement with the UI behavior. Defer unless user reports a server-side validation surprise.
- **Preuve lightbox next/prev navigation** — when an observation has multiple proofs, the lightbox opens the clicked one but does not allow swiping/arrow-keying through the set. Add if the user requests it; otherwise defer.
- **Chiffreur uid backfill on chiffreur path** — `devis-editor.tsx:830-841` writes only `uploadedByName` + `uploadePar` (email) when a chiffreur saves; the gestionnaire path writes uid too. Adding `uploadedBy: currentUser.uid` symmetrically would unblock future features (deep-link to chiffreur profile via `UserNameLink`). Out of scope this iter.
- **Legacy chiffrage attribution gap** — docs created before the attribution was added show no "Chiffré par" footer (by design). A one-shot derivation could backfill from `chiffrages.assignedChiffreurNom` keyed by `storagePath`. Defer unless user requests visible attribution on legacy docs.
- **Manual agent location not wired to feasibility** — `agentLocationManuel` is now persisted on the planification doc, but `useAtgFeasibility` consumes lat/lng coordinates, not free-form strings. To actually use the manual location for ATG feasibility/routing, server-side geocoding (e.g. the existing `/api/reverse-geocode` route inverted to a forward-geocode call) is needed. Defer unless user reports the manual value is ignored downstream.
- **Proposition mis-slotting within accord-2+ section** — `devis-editor.tsx` `handleSave` derives `accordKind` from column state, not URL `accordSlot`. A 2ème PROPOSITION save lands in `Devis 2ème accord` slot — section is correct (accord 2 et +) but the slot label is wrong. Recommend seeding the auto-added column's `kind` from `parsedAccordSlot.kind` when present. Defer unless user reports.
- **Cardinal-slot re-edit loads base data** — re-opening a saved cardinal slot loads `chiffrageEditables[docType]` (base entry), not `chiffrageEditables[targetDocType]` (e.g. `Devis 2ème accord`). So re-editing shows base devis data, not the saved 2ème accord. Defer unless user reports.
- **firestore-rules hotfix (user-committed)** — outside the loop, the user committed `fcab3bf [firestore-rules-hotfix] Allow public read on /users to unblock login`. Not part of this batch; flagged here for traceability so the loop doesn't trip over the dirty state on next iteration (it's now committed, no longer dirty).
- **`DeadlineBar` dead code** — defined inline in `assignations-atg/page.tsx` but unused. Could be deleted; defer unless code-hygiene sweep.
- **Separate `getDeadlineInfo` in assignations-chiffrage** — has different semantics (different SLA?). If the user reports similar lateness-display bugs in the chiffrage queue, the same 8am-anchor pattern likely exists there. Inspect when surfaced.
- **Utilisateurs row-click is now whole-row active** — implementer wired the whole row to navigate to `/utilisateurs/[uid]` with `stopPropagation` on the password and action cells. If a stricter "pencil-only" affordance is preferred, the row click can be reverted and a Link wrapped around the pencil icon only. Defer unless user reports.
- **Route-level enforcement of `deniedNavItems`** — the current implementation hides denied items from the sidebar but does not block direct URL access. A determined user could navigate to e.g. `/utilisateurs` by typing the URL. To fully enforce, add a server-side or middleware check on each route. Defer unless user reports.
- **Composite Firestore index for rappels read-receipt query** — `dossiers/[id]/page.tsx` queries `rappels` by `(recipientUid, dossierId, read)`. Small datasets work without an index; under heavy load Firestore may demand a composite index. Add when the query is observed to slow down or error.
