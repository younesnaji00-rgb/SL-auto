# Batch 2026-05-13 — Loop-driven (one task per firing)

## Work request
(see verbatim block in plan file `C:/Users/pc/.claude/plans/you-are-driving-a-frolicking-seahorse.md`)

Summary of items in the batch (for human reference only — NOT a task queue):
- A. documents (création de mission > gestion des dossiers): list document types beyond accord/2ème accord
- B. deadline counter dd/hh lateness display (both chiffrage + ATG)
- C. ATG assignation card: remove deadline bar, embed assuré phone + click-to-call CTA
- D. ATG planification: click location → opens Google Maps / device map app
- E. ATG photos: bottom-left watermark (timestamp-to-second, GPS, account name, "SL auto" title)
- F. Observations visibility-scope dropdown ("à tous" default) + view stamps (who saw, when)
- G. Single-device hard block: backend evicts OLD device on new login (currently blocks the new one)
- H. Admin / directeur* roles: "Éditer web" button directly inside gestion des dossiers (no need to go via assigner au chiffrage)
- I. Gestion des dossiers: per-column header filters (excluding ref expert / assuré / réf compagnie / matricules), preserved search + date filters, jour/semaine/mois/personnalisé date filter parity with suivi d'équipe, status filter rebuilt with the new automatic statuses, stackable filters + reinit button, sort-by-creation asc/desc filter
- J. Historique > dates clés sidebar: make the pen-icon manual-entry lightbox actually save to dates clés (currently front-end placeholder only)
- K. Historique > pièces jointes download: include photos, with three sub-folders (photos avant / en cours / après); fix garbled folder names; rename master folder to `${refCompagnie} ${assuré} ${compagnie}`
- L. Tampons: button to assign a specific saved stamp to each chiffreur individually
- M. Jours fériés: add file-picker import; AI scans screenshot of holiday list and adds dates to the ignore-list
- N. Chiffrage web editor table: add "observation" column with dropdown (sans réserve / non accordé / hors sinistre); reorder base columns to `reference > designation > type > quantité > PUHT > prix total HT > TVA > vétusté` then accord triple; when chiffreur picks "proposition", final saved doc shows extra "accord 2ème expert" column

## Global context
- Stack: Next.js (App Router) + Firestore, dev port 9002
- Outer repo: `c:/Users/pc/Downloads/SL-auto-main/` on `auto-2026-04-30`
- Inner repo (app source): `c:/Users/pc/Downloads/SL-auto-main/SL-auto-main/` on `auto-2026-04-24`
- Verify: `cd SL-auto-main && npm run typecheck && npm run build`
- Commit: inner first → outer pointer bump. Never push.

## Locked assumptions
- **A1 (work-folder & branch):** Per user 2026-05-13, the "do not touch inner folder / wip-batch-1 branch / Co-Authored-By trailer" footer of the work request is rescinded. Work in inner repo on `auto-2026-04-24`, commit-inner-then-bump-outer pattern, no push.
- **A2 (lateness rendering):** Format is `02j/14h` (zero-padded, French unit suffixes, slash separator). User-confirmed.
- **A3 (lateness semantics):** Lateness measured in business hours (consistent with the 24-business-hour deadline). 1 lateness "day" = 24 business hours. So 38 business hours past deadline → `01j/14h`.
- **A4 (loop discipline):** No queue. Each firing discovers exactly one next task.

## Completed
- [x] **B — `[deadline] dd/hh lateness display when en retard`** — inner `2784c47` on `auto-2026-04-24`, outer `c8e879d` on `auto-2026-04-30`. Files: `src/components/deadline-bar.tsx`, `src/lib/business-days.ts`, `src/app/(app)/assignations-atg/page.tsx`, `src/app/(app)/assignations-chiffrage/page.tsx`. Verify clean.
- [x] **D — `[atg] click location opens map app`** — inner `4c4650f` on `auto-2026-04-24`, outer `c7be39b` on `auto-2026-04-30`. Files: `src/app/(app)/assignations-atg/page.tsx`, `src/app/(app)/assignations-atg/[dossierId]/page.tsx`. Wraps `p.adresse` only; added `stopPropagation` so tapping the address doesn't trigger the parent card's row-click. Verify clean.
- [x] **C — `[atg] replace deadline bar with assuré phone click-to-call`** — inner `bcef142` on `auto-2026-04-24`, outer `be67d2f` on `auto-2026-04-30`. Files: `src/app/(app)/assignations-atg/page.tsx`, `src/app/(app)/assignations-atg/[dossierId]/page.tsx`. Added `<AssurePhoneLink>` helper; reads `dossier.assure.telephone` (fallback `telephone2`); `tel:` href with cleaned digits; `"—"` fallback; `stopPropagation` preserved. Detail view: added phone row to header strip (no prior deadline bar there). Inline `DeadlineBar` / `getDeadlineInfo` left in place per scope. Verify clean.
- [x] **H — `[gdd] Éditer web button for admin/directeur roles in gestion des dossiers`** — inner `09668a9` on `auto-2026-04-24`, outer `5fd9d6d` on `auto-2026-04-30`. File: `src/app/(app)/dossiers/client-page.tsx`. Role enum strings used: `Admin`, `Directeur`, `Directeur des opérations`, `Directeur technique`. Button navigates to `/assignations-chiffrage/{currentChiffrageId}`. Disabled with tooltip when no chiffrage assigned. Verify clean.
- [x] **A — `[gdd-mission] show all document types in création de mission`** — inner `1bc711c` on `auto-2026-04-24`, outer `04a9fc7` on `auto-2026-04-30`. Files: `typed-documents-grid.tsx`, `step-4-pieces.tsx`, `dossiers/[id]/page.tsx`. Added `showAllNonAccordSlots` prop to surface 9 previously-hidden doc types (Rapport final, Réforme technique/économique, PV-Constat, Carte grise, Attestation d'assurance, Kilométrage, Numéro de chassis, Autre). Accord/2ème accord slots untouched; ATG's `hideAccordSlots` view unaffected. Verify clean.
- [x] **J — `[historique] dates clés manual-entry lightbox saves to Firestore`** — inner `dca8e76` on `auto-2026-04-24`, outer `f0fe12c` on `auto-2026-04-30`. File: `src/app/(app)/dossiers/[id]/historique-tab.tsx`. **Surprise:** literal `updateDoc` already worked — the failure mode was silent no-ops on empty/invalid input, perceived as "not working." Added explicit validation toasts (empty / parse-failed / NaN) so feedback is now visible. Sidebar updates reactively via existing `onSnapshot`. Fields used: `dateSinistre` / `dateRequete` (Timestamp) + sibling `*TimeKnown` boolean, mirroring AI write at `step-1-import.tsx`. Verify clean.
- [x] **G — `[auth] single-device: new device wins, old auto-signs-out`** — inner `0bc5da4` on `auto-2026-04-24`, outer `20476d9` on `auto-2026-04-30`. Files: `src/app/layout.tsx`, `src/app/login/page.tsx`, `src/hooks/use-current-user.tsx`. New device generates session id, writes to `users/{uid}.currentSessionId`, stores locally, proceeds. Old device's onSnapshot observes mismatch → `signOut()` + sets `EVICTED_FLAG_KEY` for login page to render "Vous avez été déconnecté…" toast. `LOGIN_IN_FLIGHT_KEY` sessionStorage gate prevents new-device self-eviction. **Surprise:** the "accès bloqué" toast wasn't in HEAD — must have been a cached older bundle. Most plumbing was already staged from a prior incomplete iteration; this iteration finalized it and absorbed the dirty working tree. Verify clean.
- [x] **K — `[historique-pj] photos + folder names + master folder rename`** — inner `198f585` on `auto-2026-04-24`, outer `b14d88e` on `auto-2026-04-30`. File: `src/app/(app)/dossiers/[id]/documents-tab.tsx`. Master folder: `${refCompagnie} - ${assure} - ${compagnie}` (NFC-normalized, illegal chars → `_`, fallback `documents-${id}`). 3 sub-folders: `photos avant`, `photos en cours`, `photos après`. Photo categorization read from `dossiers/{id}/photos` subcollection's `category` field. Accent fix: replaced over-greedy regex with explicit illegal-char sanitization + `platform: 'UNIX'` on JSZip. New "Inclure photos" CTA next to existing button. Per-photo fetch failures caught and zip still ships. **Note:** the user said "pièces jointes" but the actual button lives in the Documents tab (no separate PJ tab exists). Verify clean.
- [x] **L — `[tampons] per-chiffreur stamp assignment`** — inner `2a161af` on `auto-2026-04-24`, outer `6318735` on `auto-2026-04-30`. File: `src/app/(app)/tampons/page.tsx`. Stamps stored top-level at `stamps/{stampId}` with Storage-backed URLs. New "Assignation par chiffreur" card lists each Chiffreur user (`role='Chiffreur'`) with a stamp-select. Writes `users/{uid}.assignedStampId` (string or `deleteField()`). Each stamp row annotated with "Assigné à : …" for reverse visibility. **Note:** there's a parallel `chiffreurs` collection (label/contact mirror), but assignment lives on the auth-bound `users` doc since that's what a PDF generator would resolve from `auth.currentUser.uid`. Verify clean.
- [x] **N1 — `[devis-editor] add observation column with 3-option dropdown`** — inner `7644195` on `auto-2026-04-24`, outer `2adffae` on `auto-2026-04-30`. Files: `src/lib/devis-schema.ts`, `src/components/chiffreurs/devis-editor.tsx`. New `DevisRow.observation?` field with enum `'sans_reserve' | 'non_accorde' | 'hors_sinistre'` + `OBSERVATION_LABELS` map. `<th>Observation</th>` inserted after "Prix en TTC", before `extraColumns`. Width 130px. Clear option via `__none__` sentinel mapping back to undefined. Totals-row placeholder cell added to keep column count aligned. PDF not modified. Verify clean.
- [x] **M — `[jours-feries] AI screenshot import`** — inner `97f1620` on `auto-2026-04-24`, outer `40aeb95` on `auto-2026-04-30`. Files: new `src/app/api/scan-holidays/route.ts`, modified `src/app/(app)/jours-feries/page.tsx`. New "Importer image" button → POST to `/api/scan-holidays` → Gemini extracts dates → batch-add to `options_holidays` collection (one doc per date, mirrors existing manual-add). Duplicate suppression via existing label-uniqueness. Loading state + error toast. **Model used:** `googleai/gemini-3-flash-preview` (matches existing API routes, NOT `3.0-flash` from stale memory note — memory file itself is correct, MEMORY.md one-liner was stale and now fixed). Verify clean.
- [x] **E — `[atg-photo] bottom-left watermark on ATG photos`** — inner `362b2c3` on `auto-2026-04-24`, outer `a4df401` on `auto-2026-04-30`. Files: new `src/lib/photo-watermark.ts`, modified `src/app/(app)/assignations-atg/[dossierId]/page.tsx`. 4-line stack (SL auto / timestamp / GPS / `${prenom} ${nom}`) in semi-transparent black rect, bottom-left, font scaled by image width. Applied in `handleUploadFiles` BEFORE the offline-queue upload. Geolocation 5s timeout → `"Position non disponible"` fallback. ATG-only (other photo flows untouched). Verify clean.
- [x] **N2 — `[devis-editor] reorder base columns`** — inner `180ab84` on `auto-2026-04-24`, outer `246aa6d` on `auto-2026-04-30`. File: `src/components/chiffreurs/devis-editor.tsx`. Pure reorder: header (lines 1117-1126), tbody mapping (1235-1350), sticky totals (1435-1444) all moved as units. Final base order: REF → Designation → Type → Quantite → P.U.H.T → Total H.T → T.V.A → Vetuste → Prix en TTC → Observation → (dynamic accord triple unchanged). 33 ins / 33 del — perfect symmetry, no content drift. PDF not modified. Verify clean.
- [x] **N3 — `[devis-pdf] accord 2eme expert column on proposition`** — inner `33da861` on `auto-2026-04-24`, outer `63588e9` on `auto-2026-04-30`. File: `src/lib/devis-pdf.ts` (actual canonical path; +14 lines). Discriminator: per-extra-column `kind === 'proposition-accord'` (not a top-level docType arg). Appended `Accord 2eme expert` column header + empty cell on every row when proposition-accord extras present. Column width: 32mm; Designation `auto` absorbs the extra space → no overflow on landscape A4. Totals row uses manual `pdf.rect`/`pdf.text` — no autoTable cell to add. Verify clean.
- [x] **F1 — `[observations] visibility-scope dropdown on entry`** — inner `2ebb225` on `auto-2026-04-24`, outer `4e5a7ad` on `auto-2026-04-30`. Files: `src/app/(app)/dossiers/[id]/log-observation.ts`, `src/components/observations-tab.tsx`. Added `VisibilityScope = 'all' | 'gestionnaire' | 'chiffreur' | 'agent_terrain'` type + optional param to `addObservation()`. Default `'all'`. `<Select>` with Eye icon next to textarea: À tous / Gestionnaire / Chiffreur / Agent de terrain. Persisted to `dossiers/{id}/observations/{obsId}.visibilityScope` and mirrored on `lastObservation`. Auto-loggers (planif modal, ATG planif observation, chiffrage accord mail) keep working — they pass no scope so it defaults. Verify clean.
- [x] **F2 — `[observations] filter list by visibilityScope`** — inner `61eab0d` on `auto-2026-04-24`, outer `79da009` on `auto-2026-04-30`. File: `src/components/observations-tab.tsx`. New `canSeeObservation` helper + `.filter` on the existing useMemo. Author match on `authorEmail === profile.email` (no `authorUid` exists). Role-to-scope: `'Gestionnaire'`→`gestionnaire`, `'Chiffreur'`→`chiffreur`, `'Agent de Terrain'` (capital T) →`agent_terrain`. Super-roles (`Admin`, `Directeur`, `Directeur des opérations`, `Directeur technique`) see all. Single-site filter — all other observation surfaces flow through `<ObservationsTab>`. Verify clean.
- [x] **F3 — `[observations] view-stamps`** — inner `8bffebb` on `auto-2026-04-24`, outer `cccae6f` on `auto-2026-04-30`. File: `src/components/observations-tab.tsx` (+73/-1). Map-shaped `viewedBy: { [emailKey]: { name, role, viewedAt } }` written on mount via `setDoc({merge:true})` for each visible non-author observation, deduped via in-session `useRef<Set<string>>`. Email key: `email.replace(/\./g, '_')`. Render caption "Vu par X (13 mai 14:30), Y..., +N autres" under each card, omitted when empty. Author self-view skipped. Verify clean.
- [x] **I1 — `[gdd] status filter rebuild + reset button`** — inner `ce37507` on `auto-2026-04-24`, outer `995d510` on `auto-2026-04-30`. File: `src/app/(app)/dossiers/client-page.tsx`. Status options sourced from `CANONICAL_STATUTS` (`src/lib/dossiers-data.ts`) + `augmentWithLiveValues`, not from Firestore `options_statuts` (which had stale legacy entries). 16 canonical automatic statuses surfaced in workflow order. New always-visible `Réinitialiser` ghost button with `RotateCcw` icon — clears every filter (search, nature, status, compagnie, observation, dateFrom, dateTo, rowsPerPage) + resets page to 1. Verify clean.
- [x] **I4 — `[gdd] sort-by-creation asc/desc`** — inner `3300ee5` on `auto-2026-04-24`, outer `8a61038` on `auto-2026-04-30`. File: `src/app/(app)/dossiers/client-page.tsx`. New `sortByCreation: 'desc' | 'asc'` field in `filterDefaults` (so iter-18's reset handler covers it automatically). Tolerant timestamp coercion: `.toMillis() / .toDate().getTime() / new Date()` fallback. Sort lives at the tail of `dossierList` useMemo, so the existing Firestore `orderBy('createdAt','desc')` query stays intact. Verify clean.
- [x] **I2 — `[gdd] date filter parity with suivi d'équipe`** — inner `b7cbdf8` on `auto-2026-04-24`, outer `ca0019b` on `auto-2026-04-30`. File: `src/app/(app)/dossiers/client-page.tsx` (+70/-4). Replicated monitoring page's 4-button preset row (Jour / Semaine / Mois / Personnalisé) — not a `<Select>`. Format: `yyyy-MM-dd` strings. Semaine uses `startOfWeek(now, { locale: fr })` + `endOfDay(now)` to match monitoring's behavior. Custom-range pickers auto-set `datePreset = 'personnalise'` on edit so highlight stays consistent. New `datePreset` field added to `filterDefaults` → iter-18 reset clears automatically. Suivi d'équipe page identified at `src/app/(app)/monitoring/page.tsx`. Verify clean.
- [x] **I3 — `[gdd] per-column header filters`** — inner `f611c21` on `auto-2026-04-24`, outer `71ec237` on `auto-2026-04-30`. File: `src/app/(app)/dossiers/client-page.tsx` (+156/-57). 4 filters moved into column headers: `nature` → Nature column, `status` → Statut column, `compagnie` → Compagnie column, `observation` → Observation column. Each header now shows label + `Filter` icon button → popover. Icon goes `text-primary` + filled when filter active. `OptionsManagerModal` admin button moved into each popover. Excluded columns (refExpert, assure, referenceCompagnie, matricule, matriculeAnterieur, typeDossier, dateSinistre, dateRequete) render plain labels. Top bar now holds only: search, date-preset row, custom-range pickers, sort-by-creation, reset. Filter pipeline / clearFilter / filterDefaults untouched. Verify clean.

## Current
(empty — **work request fully delivered, loop stopping**)

## Batch summary
All 21 iterations of the 15-feature batch completed across 19 commits (some features split into sub-iterations N1/N2/N3 and F1/F2/F3 and I1/I2/I3/I4). Final tally: A, B, C, D, E, F1+F2+F3, G, H, I1+I2+I3+I4, J, K, L, M, N1+N2+N3 — all green. Verify clean every iteration. No pushes anywhere.



## Follow-ups
- **B-fup1:** `formatBusinessLateness` returns empty string for input < 24, so a dossier 0–23 business-hours past deadline still shows bare `En retard` (no duration). If user expects e.g. `00j/05h` from the moment "En retard" appears, drop the `< 24` early-return. Park until user comment.
- **D-fup1:** Only `adresse` (street) was made clickable, not `zone` (area label e.g. "Casablanca Centre"). If user wants area-level navigation too, easy 5-min follow-up.
- **C-fup1:** Inline `getDeadlineInfo` / `DeadlineBar` definitions and `dossierLive`/`deadlineSortByGroup` plumbing on the ATG list page are now dead code (no callers after C). Safe to remove on a future cleanup pass; not removed now to keep this commit surgical.
- **C-fup2:** The B iteration's ATG-side lateness display (added `formatBusinessLateness` call inside the inline DeadlineBar) is now also dead on ATG since the bar is no longer rendered. Lateness still works on chiffrage. Not an issue, just noting waste.
- **H-fup1:** The H button is disabled (with tooltip "Éditer web (aucun chiffrage assigné)") on dossiers without a chiffrage. If user expects the privileged roles to be able to CREATE a chiffrage from this button too, that's an extension — not implemented now.
- **H-fup2:** `Dossier` TS type doesn't declare `currentChiffrageId`; H uses `(d as any).currentChiffrageId`. Easy type cleanup follow-up.
- **A-fup1:** `rapportSlots` was previously computed-but-unrendered (dead code). Now rendered only behind the new `showAllNonAccordSlots` flag. Consider rendering it on the default step-7 ("Rapport") flow too — separate decision.
- **J-fup1:** Pen icon currently gated to `dateRequete` / `dateSinistre` only (`AI_SOURCED_DATE_FIELDS`). Other dates in the "Dates clés" card show no pen → no manual entry path. Extend if needed.
- **J-fup2:** Unused `displayUserName` import at line 24 of historique-tab.tsx. Cosmetic cleanup.
- **K-fup1:** "Inclure photos" pulls ALL photos in the dossier. Future selection UI could let user pick which photos. Out of scope for now.
- **L-fup1:** The READ side (e.g., devis-pdf.ts when generating accord PDFs) doesn't yet consume `users/{uid}.assignedStampId` — assigned stamps won't actually appear in outputs until the read site adopts the field. Easy 1-file follow-up.
- **N1-fup1:** `devis-pdf.ts` doesn't render the `observation` column. If user wants the observation in the printed accord/proposition PDF, that's the next step.
- **N-deferred:** ~~N2~~ done (iter-13) and ~~N3~~ done (iter-14).
- **F2-fup1:** Author identity on observations is email-only (no `authorUid`). If you ever rename a user account / change their email, their old observations stop being treated as "own". Out of scope to fix.
- **F2-fup2:** No "scoped" badge on observation cards — a user can't tell at a glance whether an observation is restricted. Cosmetic follow-up.

## Amendment log
(populated during execution)
