# Suivi d'équipe + utilisateurs follow-ups

## Work request

> i added a new compagnie from the sidebar and it didnt show in repartition par compagnie in suivi d'equipe
>
> there are two doubled utilisateurs in activite par status, and its my name being doubled
>
> in suivi d'equipe in par utilisatuer add a filter per user role
>
> the logos i imported for each compagnie disappeared
>
> the date mission agent de terrain in gestion des dossiers shouldnt be written as ATG it should be AT in the table, and no need for photos avant en cours and apres
>
> remove the zone de danger part in utilisateurs, and only leave out the delete user option instead of desactiver, and when i delete the user all the work done should still be saved with logs of that user

## Global context

- Stack: Next.js 14 (app router) + Firestore + React + TypeScript + Tailwind. Firebase Hosting.
- Branch: `auto-2026-04-24` (continuing the existing loop branch; prior loop completed at `976db6d`).
- Verify command: `npx tsc --noEmit` from inside `SL-auto-main/`.
- Files in scope:
  - `src/app/(app)/monitoring/funnel.ts` (compagnies arg)
  - `src/app/(app)/monitoring/page.tsx` (useCompagnies, dedupe, role filter, roleByKey)
  - `src/app/(app)/monitoring/dossier-drawer.tsx` (UserLookup type extension)
  - `src/app/(app)/dossiers/client-page.tsx` (column rename + 3 column removals)
  - `src/app/(app)/utilisateurs/[uid]/page.tsx` (drop désactiver, keep delete only)
  - `src/app/(app)/compagnies/client-page.tsx`, `src/components/layout/sidebar.tsx`, `src/components/layout/compagnie-logos-preload.tsx` (onError fallback on logo `<img>`s)
- Out of scope: actually recovering disappeared logo data (data-side issue requiring live Firestore inspection); changing the funnel/dossier writers beyond what the column changes touch.

## Locked assumptions

- 8 monitoring steps stay as currently defined (creation → photosAvant → photosEnCours → photosApres → accord → facture → rapportValide → rapport).
- Compagnie list for "Répartition par compagnie" remains scoped to the user's allowed compagnies (filter context, per memory).
- Per-utilisateur dedupe groups by resolved display name from `userLookup` (uid OR email mapping).
- Role filter options come from `roles` in `src/lib/dossiers-data.ts:116`.
- "Zone de danger" card is replaced with a smaller delete-only block; désactiver is removed entirely. The `statut: 'Inactif'` login gate stays for backwards compat with any user that already has it.
- Logo onError fallback is defensive UX — the underlying data-loss diagnosis is a follow-up outside the loop.

## Completed

- [x] **at-rename-and-photos-cols** — Rename "ATG" → "AT" + remove three Photos columns — `7da2181`
- [x] **users-dedupe-by-name** — Merge per-user rows that resolve to the same display name — `5f869c4`
- [x] **compagnies-fill-zero-rows** — Show every assigned compagnie in "Répartition par compagnie", even with zero dossiers — `48b9c3a`
- [x] **logo-onerror-fallback** — Defensive `onError` handler so broken `logoUrl` falls back to the `<Building2>` icon — `45e7cbe`
- [x] **role-filter-par-utilisateur** — Add a "Rôle" Select filter above the Par utilisateur table — `d551044`
- [x] **utilisateurs-delete-only** — Drop Zone de danger / désactiver; keep delete only with log-preservation note — `be0643a`

## Current

## Follow-ups

- Underlying compagnie-logo data-loss is not yet diagnosed — needs runtime Firestore + Storage inspection. The defensive fallback masks the broken-image icon but doesn't restore the lost logos; the user re-uploads via the existing click flow.
