# Multi-area UX rework — round 5 (plan-driven loop)

## Work request

> in suivi d'equipe, instead of there being text on each card, make each card show a horizontal column graphs, and add another bar for en retard so now its a total of three, 1st realise en delai, hors delais, and non realise
> but for mission creees there should only be realise en delais and hors delais, to know if its hors delais, it'll be shown in the first document in document import, theres a section called date de mission, if the gestionnaire creates a dossier which is mission creees in the card in suivi d'equipe not in the same date as in date de mission (or date requete) from the imported document to be scanned by the ai which will fill the user's info, then the mission shall be hors delais
> as for the rest of cards, it should work on a deadline for about 24 hours, so if a photos section wasn't filled by the agent de terrain through 24 hours then its hors delais, same thing for chiffrage
>
> and for the columns for non realise and hors delais, instead of showing only the ones for the entire day, it should show the ones for all times
>
> the date filter should default to the current day all the time, then the user may toggle between a view for the week, month, and a custom one by selecting the dates
>
> now for the succession of the cards:
> 1 missions creees,
> 2 expertise avant,
> 3 1er accord or 1ere proposition... meaning the next card after expertise avant shall only show the first accords and propositions, and if the gestionnaire creates on the plus icon then the count for that specific card shall not reset,
> 4 expertise en cours,
> 5 2eme accord et + ... this is where it gets different, this is where the count resets for each plus sign clicked, but only after the 2eme accord and proposition, meaning the 1er accord et proposition will not be affected by this
> 6 expertise apres
> 7 facture validee
> 8 rapport valide
> 9 rapport depose
> 10 note d'honoraire
>
> the count for the other cards aside from missions creees will not be the same as the others, for the others for planification avant/ en cours/ apres, they shall only be depending on wether a planification was created for them or not for the three types respectively, same thing for chiffrage, it should only depend on wether a "envoye vers chiffrage" button was clicked or not, there could be a 100 dossiers created but if the gestionnaire didn't assign anything to anyone then the other cards shall have 0
>
> in the informations tab that will be reflected in both the table in gestion des dossiers and the detailed page inside of it, in immatriculation specifically, sometimes there are actually two numbers pulled from the carte grise: immatriculation anterieur and numero d'immatriculation, the first one is the old one and the second one is the definitive one, sometimes only one is provided and sometimes they're both provided, so the immatriculation column should be split into two and another cell should be added in the informations tab
>
> the observations from now on will no longer be written independently but instead will be preset, the presets arent currently decided so we'll stick to the same gear icon that allows me to add new presets without manually writing code, this should be a source of truth for the database and shall since in every observation dropdown throughout the entire app
> lets start with some presets that i currently know of and will be updated now: assure injoignable, vehicule hors ville d'expertise, autre
>
> now for the timeline inside gestion des dossiers:
>
> the 1st step import document should be called creation de mission instead, and remove the observation step and actually put it next to either planification or chiffrage depending on who made the observation, the observation should be added as a row on the table in gestion des informations and be put in the top bar when the gestionnaire clicks on the detailed tab of the dossier and should also be added as a filter par type d'observation in the same card as filtrer par status, and it shall be highlighted in the table,
> this same step that will be called creation de dossier will include the informations part and pieces jointes, but the pieces jointes would only be everything besides devis and facture garage and reforme cards, and include everything else,devis and facture garage and reforme cards will be saved for later
>
> the second step after creation de mission should be planification avant, and below it will have all the photos avant, so all the photos avant en cours and apres shall be separated from pieces jointes, and the photos shall be saved with their timestamps as well, meaning when the photos were taken
>
> the third step should be accord, which will have devis garage and devis propose and accorde and same thing for the facture but with no + signs, meaning its stuck in 1er accord and proposition, it should have the reforme cards as well
>
> the fourth step shall have planification en cours, which will include the photos en cours below it with the time stamps
>
> the fifth step shall be 2eme accord et +, this is where the + pimple like signs will be back, where it grows in an ordinal order in each row infinitely only after the chiffreur catches up to each row
>
> the sixth step should be photos apres
>
> the seventh should be rapport, and thats where the validation rapport be, after that the gestionnaire will be allowed to export the rapports from it
>
> the eight step should be note d'honoraire, leave it empty for now
>
> and lastly for the historique button that shows the right side bar, it should include these dates:
> date reception mission which is date requete
> date sinistre
> date creation mission which is when the dossier is created by the gestionnaire
> date demande expertise which is when the gestionnaire creates a planification, this date should be separated depending on wether its avant en cours and apres
> date expertise, which is when the agent de terrain adds pictures and it also should be separated depending on wether its avant en cours and apres
> date reception devis is when the gestionnaire or agent de terrain fills in the devis garage card
> date reception facture
> date reception devis 2,3 ...
> date reception facture 2,3... (of course this date should be separated depending on the ordinal order for both facture and devis)
> date accord devis 1er, 2eme... each separated
> date validation facture
> date envoie accord devis, this is when it gets send through email
> date envoie rapport
> date validation rapport
> date depot rapport
> date depot note d'honoraire

## Global context

- **Stack**: Next.js 15 (App Router, turbopack) · React 19 · TypeScript · Firebase 11 · Tailwind · Radix UI · recharts · Genkit AI.
- **Repo layout**: outer wrapper repo at `c:/Users/pc/Downloads/SL-auto-main/`; inner Next.js app is a **git submodule** at `SL-auto-main/`.
- **Outer branch**: `auto-2026-04-30`. **Inner submodule branch**: `auto-2026-04-24`.
- **Verify command**: `cd SL-auto-main && npm run typecheck` (run from outer repo root).
- **Commit policy**: each task makes commits in BOTH repos with matching `[short-tag] description` messages — first inside the submodule on `auto-2026-04-24`, then in the outer repo on `auto-2026-04-30` to bump the submodule pointer. Never push, never force-push.

## Locked assumptions (round 5)

- **First area shipped**: Observation presets. User confirmed via AskUserQuestion.
- **New Firestore collection**: `options_observations` (matches `options_*` convention).
- **Initial seed values**: `Assuré injoignable`, `Véhicule hors ville d'expertise`, `Autre`.
- **Out of scope for round 5 first task**: replacing observation fields elsewhere (planif modal, chiffrage modal, etc.) — those will be discovered as separate single-task iterations.
- **Out of scope of the loop entirely** (until user reopens): Suivi d'équipe charts (area #1), immatriculation split (area #2), timeline restructure (area #4), historique dates expansion (area #5), card count rules + hors-délai logic (area #6) — covered in later iterations of *this* loop, one tiny task at a time.

## Completed

- [x] **[options-observations] Add observations preset collection + dropdown in observations-tab** — outer `c8967da`, inner `f9909ce`
- [x] **[options-observations-planif] Apply observation preset Select to planification modal** — outer `0369f80`, inner `feccd41`
- [x] **[options-observations-atg] Apply observation preset Select to ATG assignation row editor** — outer `34734d9`, inner `4c9253c`
- [x] **[immat-prev-scan-schema] Add previousRegistration to AI scan schema + prompt** — outer `4e7c3a2`, inner `367e8fb`
- [x] **[immat-prev-field-map] Wire previousRegistration through step-1-import field mapping** — outer `a4e3897`, inner `b442657`
- [x] **[immat-prev-info-tab] Render Immatriculation antérieure cell in Information tab** — outer `8d221e3`, inner `4d6949a`
- [x] **[immat-prev-table-column] Split Matricule into two columns in dossiers list** — outer `5107203`, inner `8933296` (also added `immatriculationAnterieur?: string` to `Vehicule` type to satisfy typecheck)
- [x] **[timeline-step1-rename] Rename timeline step 1 "Document import" → "Création de mission"** — outer `2480db9`, inner `bee1e68`
- [x] **[historique-dates-cles] Add Dates clés section to historique side panel** — outer `352f5dc`, inner `58e6038`
- [x] **[date-demande-expertise-denorm] Denormalize per-type dateDemandeExpertise on planif creation** — outer `ad6e176`, inner `5a7ec09`
- [x] **[historique-dates-demande-expertise] Add per-type Date demande expertise rows to historique** — outer `c5ba097`, inner `c4e82ec`
- [x] **[historique-dates-expertise] Add per-type Date expertise rows to historique** — outer `3d26022`, inner `87db25a`
- [x] **[suivi-cards-reorder] Reorder Suivi d'équipe card sequence + rename creation label** — outer `13015d8`, inner `48ce00f`
- [x] **[suivi-card-note-honoraire] Add Note d'honoraire placeholder card to Suivi d'équipe** — outer `3b44e08`, inner `e3149a0` (also added `noteHonoraire: 7` to `STEP_TO_TIMELINE` in `dossier-drawer.tsx` for exhaustive `Record<StepKey, number>`)
- [x] **[suivi-date-filter-default-today] Default Suivi d'équipe date range to current day** — outer `2158bec`, inner `7bf0b6d` (also dropped now-unused `subDays` import)
- [x] **[suivi-date-presets] Add Jour/Semaine/Mois/Personnalisé preset toggle to Suivi d'équipe** — outer `70221a4`, inner `0a7d11a`
- [x] **[date-envoi-accord-denorm] Denormalize dateEnvoiAccordDevis on email send success** — outer `4021f50`, inner `02623a0`
- [x] **[historique-date-envoi-accord] Render Date envoi accord devis row in historique** — outer `a608b92`, inner `f284d0b`
- [x] **[observation-denorm-on-dossier] Denormalize latest observation onto dossier doc** — outer `712f0e4`, inner `d7a593d`
- [x] **[observation-filter-bar] Add Filtrer par type d'observation Select to dossiers list** — outer `63ca0fb`, inner `9378e7f` (also added `lastObservation?: {...}` to `Dossier` type to satisfy typecheck; chips + reset-all extension included)
- [x] **[observation-table-column] Add Observation column to dossiers list with highlight** — outer `51c6ac3`, inner `800ec33`
- [x] **[observation-detail-topbar] Render lastObservation Badge in dossier detail top header** — outer `0b8cd0d`, inner `081ca82`
- [x] **[suivi-hors-delai-creation] Add hors-délai predicate for création step in funnel.ts** — outer `f5b74a0`, inner `a02177b` (also added `dateRequete?: any` to `FunnelDossier`)
- [x] **[suivi-hors-delai-photos] Implement 24h hors-délai SLA for the three photo steps** — outer `af1f49d`, inner `e8639bc` (added `photoHorsDelai` helper + `SLA_24H_MS` constant; extended `FunnelDossier` with the three demande-expertise fields. **Note**: those fields ARE already populated by the iteration 10 denorm — subagent's follow-up doubt was outdated.)
- [x] **[suivi-hors-delai-accord] Implement 24h hors-délai SLA for accord step** — outer `1fd8f79`, inner `cc8dbc8`
- [x] **[suivi-hors-delai-aggregator] Add computeStepCountsHorsDelai aggregator to funnel** — outer `1bc88ca`, inner `f598269`
- [x] **[suivi-kpi-card-bar-chart] Convert KpiCard text display to 3-bar horizontal chart** — outer `9f1f8ce`, inner `6e2d54f`
- [x] **[timeline-step-note-honoraire] Add Note d'honoraire as timeline step 8 (empty placeholder)** — outer `d3f4459`, inner `b3d3a2d`
- [x] **[suivi-drawer-hors-delai-mode] Add 'horsDelai' DrawerMode and wire amber-segment click** — outer `8cfdc7d`, inner `abf281d` (drawer mode prop narrows back to 'realise' until DossierDrawer gets dedicated header — flagged as follow-up)
- [x] **[suivi-drawer-header-hors-delai] Add 'hors délai' header to DossierDrawer** — outer `7bebd61`, inner `4d42395` (closes the prior cast follow-up; `mode={selectedStepMode}` flows through cleanly now)
- [x] **[photos-tab-show-timestamp] Show upload timestamp on each photo card** — outer `db712c7`, inner `6ab6bdf`
- [x] **[historique-date-depot-note-honoraire] Add Date dépôt note d'honoraire row to historique** — outer `dee6cdc`, inner `b34e616`
- [x] **[first-accord-denorm] Denormalize firstAccordReachedAt when chiffreur saves cardinal #1** — outer `2f36a40`, inner `284d949`
- [x] **[accord-1er-step] Add accord1er sticky StepKey + relabel existing accord to 2ème accord et +** — outer `ef9f2fa`, inner `77522eb`
- [x] **[timeline-remove-observations-step] Remove standalone Observations step from dossier timeline** — outer `f56bfae`, inner `afb1a15` (also dropped now-unused `Step2Observations` import)
- [x] **[timeline-embed-observations-in-planif] Embed ObservationsTab beneath the Planification step** — outer `da19801`, inner `b9ec72d`
- [x] **[timeline-embed-observations-in-chiffrage] Embed ObservationsTab beneath the Chiffrage step** — outer `23ad8ed`, inner `0938c5e`
- [x] **[observation-row-highlight] Highlight whole table row when dossier has observation** — outer `dfbeb44`, inner `116bfc3`
- [x] **[immat-prev-dossier-tab] Add Immatriculation antérieure input to dossier-tab edit form** — outer `e81f24a`, inner `9270475`
- [x] **[immat-prev-edit-modal] Add Immatriculation antérieure to dossier-edit-modal** — outer `ee19769`, inner `dfa03a1`
- [x] **[funnel-type-tighten] Add dateChiffrage + firstAccordReachedAt to FunnelDossier** — outer `c90db93`, inner `0bb71d4`
- [x] **[timeline-rename-chiffrage-to-accord] Rename timeline step 6 "Chiffrage" → "Accord"** — outer `a596bc7`, inner `0a604e0`
- [x] **[delete-orphan-step2-observations] Delete orphaned step-2-observations.tsx component file** — outer `842a4e1`, inner `71e895b`
- [x] **[planification-tab-type-filter] Add typeFilter prop to PlanificationTab + Step3Planification** — outer `ac56984`, inner `1d725bf`
- [x] **[timeline-step4-planif-avant] Rename timeline step 4 to "Planification avant" + scope to Avant typeFilter** — outer `cbfd616`, inner `d5bb050`
- [x] **[timeline-add-planif-en-cours] Add timeline step "Planification en cours" after Accord** — outer `8d1b2d8`, inner `3c38148`
- [x] **[timeline-add-photos-apres] Add timeline step "Photos après" after Planification en cours** — outer `d836ee1`, inner `9357377`
- [x] **[timeline-cleanup-dead-step-keywords] Remove dead `2: ['observation']` entry from STEP_KEYWORDS** — outer `5702bff`, inner `9e0e2aa`
- [x] **[timeline-fold-information-into-step1] Fold the Information step into step 1 (Création de mission)** — outer `02cb5f3`, inner `9331234`
- [x] **[step-keywords-cleanup-id3] Remap STEP_KEYWORDS[3] 'information' to id 1** — outer `8a5b10a`, inner `ea6326f`
- [x] **[photos-tab-initial-category-prop] Add initialCategory prop to PhotosTab** — outer `c0443bf`, inner `2332f1d`
- [x] **[timeline-embed-photos-per-step] Embed PhotosTab in steps 4, 9, 10 with category-scoped initialCategory** — outer `3799877`, inner `9891a89`

## Current

(empty — next iteration discovers fresh)

## Follow-ups

- Non-admin writers see "Aucune observation disponible" if admins deactivate every option in `options_observations` and lose the ability to compose. Consider a graceful empty-collection message for non-admins (or seed-on-first-write fallback).
- `modal-chiffrage.tsx` does not have an observation field directly; the chiffrage flow uses `ObservationsTab` (already swapped). The remaining free-text observation surface in chiffrage is in `assignations-chiffrage/[id]/page.tsx` if at all — needs verification on a future pass.
- Multiple call sites now instantiate `OptionsManagerModal` with the same hard-coded `defaultValues` array (`['Assuré injoignable', "Véhicule hors ville d'expertise", 'Autre']`). Hoist to a constant if a third site appears.
- ATG row editor's non-edit display branch still renders raw `p.observation` text. Historical values are snapshots — fine for now.
- Planification Select has no in-dialog "clear" affordance once a preset is picked (modal `useEffect` reset on `open` is sufficient for create-flow but edit-flow can't return to empty without reopening). Add a "Supprimer la sélection" sentinel item if it becomes a user complaint.
- Git CRLF warning on `seed-options.ts` (cosmetic, pre-existing).

## Follow-ups (carried over from round 4 — surface naturally if still relevant)

- Status-machine handling for `Réforme` (which transitions allow it, what it means downstream).
- `create-dossier-dialog.tsx` line ~94 has a now-unreachable `userName`-based stale-cleanup branch.
- Dashboard collapse bucket label is duplicated locally in `page.tsx`; hoist to `dossiers-data.ts` if reused.
- Dashboard bucket sort uses `value desc, name asc`. Pinning at index 8 is a separate change.
- Zone autocomplete patched in `utilisateurs/client-page.tsx` only — other zone/option pickers may still display by Firestore `order`.
- `logHistorique` / `logWorkflow` signatures are POSITIONAL: `(db, dossierId, action, user, details?, type?)` and `(db, dossierId, action, user, userId, status?, extra?)`.
- Step 3 (information) and step 1 (import) lack first-class `logWorkflow` markers — heuristic in timeline won't match generic actions.
- Firebase access pattern is `initializeFirebase()` factory returning `{ db, ... }` — no `@/lib/firebase` re-export.
- `googleapis` v137 resolves at runtime via hoisted node_modules but is NOT in `package.json` — add explicitly before any prod deploy.
- Gmail OAuth callback uses client Firebase SDK; Firestore rules must permit `users/{uid}/integrations/gmail` writes. Production should swap to Firebase Admin SDK.
