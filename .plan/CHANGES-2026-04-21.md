# Changes — 2026-04-21 autonomous run

Branch: `auto-2026-04-21` (local-only, not pushed to `main`).
20/20 tasks completed. All commits `tsc --noEmit` clean; `npx next build` passes (14.9s, 23/23 pages, zero warnings).

## Phase 1 — Réforme feature & devis totals

- `278befe` **[#01]** Add Réforme schema (`src/lib/reforme-schema.ts`, pure compute helpers)
- `06e0b32` **[#02]** Build ReformeDialog component (loads/saves `dossiers/{id}.reforme`, live-computed totals)
- `2d3f485` **[#03]** Add Réforme button in assignations-chiffrage header (purple Scale icon, chiffreur/admin only)
- `cdaee9f` **[#04]** Totals per extra column + SR-tolerant sums (`parseFr` returns 0 on non-numeric)

## Phase 2 — Dossier timeline (6 steps)

- `749e35d` **[#05]** Timeline scaffolding (`TimelineBar` + `Timeline` + `DOSSIER_TIMELINE_STEPS`)
- `3409b60` **[#06]** `useLastStep` localStorage hook (`dossier-${id}-step`, clamped 1-6)
- `e1e44d6` **[#07]** Step 1 panel "Document import" (506 lines; reuses `/api/scan-document` + `uploadFileWithOfflineSupport`; empty-field merge only)
- `9b0b217` **[#08]** Step 2 panel "Information" (wraps InformationTab + missing-required-fields banner)
- `277b411` **[#09]** Step 3 panel "Planification" (thin wrapper on existing `planification-tab.tsx`)
- `3cf55b9` **[#10]** Step 4 panel "Pièces jointes" (stacked Documents + Photos)
- `fa5372d` **[#11]** Step 5 panel "Chiffrage" (greenfield, 399 lines; Réforme card + chiffreur-modified files; annotations per-file at `chiffrage.files[i].annotations`)
- `e4d8c45` **[#12]** Step 6 panel "Rapport" (24-line wrapper on RapportTab)
- `0d352a6` **[#13]** Wire timeline into dossier page (`page.tsx` 243→219 lines; tabs removed; Historique → Dialog)
- **[#14]** Eye icon → `/dossiers/{id}` — already correct, no commit

## Phase 3 — Creation rework (replace wizard)

- `1c5f21d` **[#15]** `createEmptyDossier` helper + "Créer un dossier" click creates empty doc and redirects to timeline
- `4540a20` **[#16]** Delete old wizard (9 files, -2659 lines; 1 stale Link in compagnies fixed)

## Phase 4 — Cleanup & verification

- `19c323d` **[#17]** Historique drawer (Dialog → right-side Sheet, `w-full sm:max-w-xl`)
- `584f562` **[#18]** Missing-field audit pass (7 required-field mappings documented; banner coverage confirmed)
- **[#19]** `next build` smoke test — PASSED in 14.9s, 23/23 pages, zero warnings. No commit needed.
- **[#20]** This CHANGELOG entry.

## Known follow-ups

- Step 5 Chiffrage: "Télécharger la sélection" and "Voir l'original" are stubs — need shared download helper + `chiffrage.files[].storagePath` → `getDownloadURL` resolver.
- Step 5 Historique dialog currently renders pretty-printed JSON as stand-in. Proper UI should list `structuredEditables.versions[]` with PDF preview per version.
- `/dossiers/[id]` route is now 54.5 kB / 480 kB First Load JS — candidate for size-trimming pass if needed.
- Step 3 Planification: `onOpenHistory` is stubbed as a no-op; `readOnly` on Step-panel props not forwarded to child tabs.

## How to deploy

```bash
git checkout main
git merge auto-2026-04-21   # or cherry-pick individual commits
git push origin main        # App Hosting auto-deploys in ~10min
```
