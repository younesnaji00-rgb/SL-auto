# Multi-feature work request (autonomous loop tracker)

## Work request

The user submitted nine items (verbatim, in their original order):

1. in gestion des dossiers, in creation de mission, in documents, add document slots for devis garage and facture garage and just that, no plus buttons, no accord or proposition

2. below programmer une mission in planification avant, there should only be photos avant and no photos en cours and apres, same thing for planification en cours as it should only have photos en cours and same thing for planification apres

3. in step 3 accord, there should only be the importer un document tab and no documents tab that enables the gestionnaire to look at things through a filter, there should be individual cards; no need for the bottom card that shows aucun chiffrage pour ce dossier

4. in 2eme accord et + it should be the same way as in accord step, meaning there should be cards presented as well, but only whatever comes after proposition accord and accord for both devis and facture, with the plus buttons that keep expanding horizontally through an ordinal order

5. since theres already an option to choose from avant en cours and apres before the user even clicks on a row, then theres no need to keep the same options when the user clicks on a mision, they should only be visible once through the main page of assignations agent de terrain

6. in the phone version in assignation agent de terrain, when the user clicks on the mission card, there should only be the option to either import documents or photos, no need to keep the table

7. for the desktop version in assignations agent de terrain, remove the table that shows date et heur rdv and zone and adress and put it next to the main title that shows the reference, put the observation dropdown menu on top of the rest of dropdown menus, and when the user tries to add a observation, right below the text fields, add the ajouter une preuve function that we removed from the table on top, between photos and documents dropdown menus, only one can be opened at a time, if the user clicks on photos while documents were already open, then documents close and photos open, and vice versa

8. remove the valider le traitement button from assignation agent de terrain since this function is only for gestionnaires

9. the agent de terrains arent allowed to see facture and devis accorde and propose, nor reforme, so only keep the initial six cards as well as facture and devis garage

## Global context
- Outer repo: `c:/Users/pc/Downloads/SL-auto-main/` on `auto-2026-04-30`
- Inner repo: `c:/Users/pc/Downloads/SL-auto-main/SL-auto-main/` on `auto-2026-04-24`
- Verify: `cd SL-auto-main && npm run typecheck` (build is fine for tiny edits if typecheck passes)
- Dev: `cd SL-auto-main && npm run dev` (port 9002)
- Commit protocol: inner first, then outer pointer bump. **Never push.**
- Stack: Next.js 15 + React 19 + Tailwind + shadcn/ui + Firebase. App router under `src/app/(app)/`.
- Prior loop (`tasks-mobile-atg.md`) just shipped a mobile redesign across `assignations-atg`; items 5, 6, 7 in this request modify that surface.

## Locked assumptions
- ATG-related code lives at `src/app/(app)/assignations-atg/page.tsx` and `src/app/(app)/assignations-atg/[dossierId]/page.tsx`.
- Mission tabs reference: `Avant`, `En cours`, `Après` (`MISSION_TABS` const in both files).
- The dossier wizard / creation flow lives under `src/app/(app)/dossiers/`; the slot system is `src/components/dossier-timeline/`.
- The slot-card / family-row component pattern (`SlotCard`, `FamilyRow`, `buildDocFamilies`) is the existing card primitive for documents.
- Some items will need clarification when their turn comes (e.g. exact step location for item 1, "2ème accord et +" anchor for item 4) — handle per-task, not up front.

## Completed

- [x] (item 8) Gate "Valider le traitement" off on the ATG section — `08269ce` (typecheck clean, 6 ins / 5 del — `canValidate` now short-circuits when `section === 'assignations-atg'`; comment + render/save guards untouched). Grep confirms button literal exists only in `observations-tab.tsx`, so this single gate suffices.
- [x] (item 5) Drop mission tabs from ATG detail; thread activeTab via `?mission=` query param — `97f1379` (typecheck clean, 10 ins / 54 del across 2 files). Desktop tabs block + mobile sticky segmented control removed; detail reads `mission` from `useSearchParams`; list-page row navigations append the param. `MISSION_TABS` const stays (drives `currentCategory`). `setActiveTab` dropped from destructure (no remaining references).
- [x] (item 6) Strip mobile ATG detail body to app bar + Photos/Document CTAs — `5c941e3` (typecheck clean, 27 ins / 310 del — mobile branch now renders only back-arrow/ref/assuré header + 2 full-width 96px CTAs with Camera/FileText icons wired to existing `setIsCameraOpen` and `handleDocFilesSelect`; desktop branch untouched).
- [x] (item 9) Hide accord/réforme variants from ATG; show base Devis/Facture Garage cards — `73b7e07` (typecheck clean, 7 ins / 1 del — added `hideAccordSlots={isATG}` and `showBaseGarageSlots={isATG}` to the desktop ATG detail's `TypedDocumentsGrid` invocation; Admin/Gestionnaire viewers still see everything).
- [x] (item 1) Step 1 (Création de mission): show ONLY base Devis/Facture Garage slots — `1c529ba` (typecheck clean, 15 ins / 5 del across 3 files — added `hideOtherSlots?: boolean` prop chain on `TypedDocumentsGrid` + `Step4Pieces`; step 1 invocation now passes `hideOtherSlots`; "Autres documents" section gated off there; other steps unaffected because they use `showOnlyAccordSlots`).
- [x] (item 2) Planification phases isolate photos to active mission category — **already implemented**, no commit needed. The dossier page (`src/app/(app)/dossiers/[id]/page.tsx`) wires `<PhotosTab dossierId={id} onlyCategory="avant" />` on step 4, `onlyCategory="en_cours"` on step 9, and `onlyCategory="apres"` on step 10. `PhotosTab` honors `onlyCategory` by filtering `visibleCategories` (line 79 of `photos-tab.tsx`) so only the matching tab renders.
- [x] (item 7 — sub a) Mutual exclusion between Photos and Documents accordions — `764037c` (typecheck clean, 2 ins / 2 del; opening Photos auto-closes Documents and vice versa; mobile branch unaffected).
- [x] (item 7 — sub b) Move Observation accordion above Photos/Documents on desktop ATG — `2626772` (typecheck clean, 11 ins / 11 del — pure JSX relocation; final order: planifications → observation → photos/docs).
- [x] (item 7 — sub c-1) Inline RDV summary next to dossier title — `7821ee8` (typecheck clean, 19 insertions; each `filteredPlans` entry renders one compact line: Calendar+date · MapPin+zone · adresse). Planifications table below remains; will be removed once preuves move into the observation editor.
- [x] (item 3) Step 6 Accord — only "Importer un document" tab + individual cards — **already implemented**, no commit needed. Dossier page step 6 already passes `onlyImportTab + showOnlyAccordSlots + hideCardinalPlus` to `Step4Pieces`, which drops the dual-tab UI and renders only accord/proposition `SlotCard`s with no cardinal-+ pimples. The "bottom card that shows aucun chiffrage pour ce dossier" mentioned in item 3 doesn't match any code I can locate — only `"Aucun chiffrage assigné"` exists, in `/assignations-chiffrage/page.tsx` (the list page, not step 6). Flagged for user clarification — may already not exist.
- [x] (item 4) Step 11 2ème accord et + — same card style as Accord step, only 2ème+ ordinals with horizontal `+` expansion — **already implemented**, no commit needed. Dossier page step 11 passes `cardinalFilter="2-plus" + showOnlyAccordSlots + onlyImportTab + requireFirstAccordFilled` to `Step4Pieces`, which (per `familiesForRender` synthesis at lines 591-613 of `typed-documents-grid.tsx`) renders 2ème accord/proposition slots once the 1ers are filled, with cardinal `+` chains for ordinal expansion. FamilyRow's 2-plus filter strips the 1ers.

## Current

(empty — loop stopped)

## Outer-repo bump

- [x] Outer `[mobile-atg-bump]` for items 1/5/6/8/9 + item 7 sub-pieces a/b/c-1 — `dbe7192` on outer `auto-2026-04-30`. Not pushed.

## Pending (need user clarification before resuming)

- **Item 7 sub-c-2** — remove the planifications table on desktop ATG. Blocked by sub-d (the table's preuves column is the only current way to manage visit-proof images).
- **Item 7 sub-d** — move "ajouter une preuve" into the observation editor. Ambiguous: the planifications-table preuves are per-planification (saved on the plan doc), whereas observations are per-dossier (with `phaseATG` tag). Two reasonable interpretations: (a) the preuves data model migrates to be per-observation, or (b) the per-planification preuves UI is mounted inline within the obs accordion section without changing storage. Need user input.
- **Item 3** "bottom card aucun chiffrage pour ce dossier" — the literal phrase doesn't match any code on the step 6 page; user may have a different state in mind. Visual check from the user would resolve.
