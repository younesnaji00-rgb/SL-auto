# SL Auto — Round 3 feedback (autonomous run)

**Session**: 2026-04-23 (Round 3 feedback)
**Branch**: `auto-2026-04-23` (cut from `redesign-2026-04-21` — NEVER push to `main`)
**Verify**: `npx tsc --noEmit` must exit clean per commit
**Plan file**: `C:\Users\pc\.claude\plans\you-are-driving-a-temporal-lark.md`

> Prior run's tasks are in `.plan/tasks.md` (tasks #01-#33, all done on `redesign-2026-04-21`). This file tracks the Round 3 additions.

## Loop protocol

1. Pick next `- [ ]` task.
2. Mark `- [>]` (in progress).
3. Dispatch a fresh general-purpose subagent with the full spec + global context + locked assumptions, preamble:
   > EXECUTE NOW — DO NOT PLAN, DO NOT WRITE A PLAN DOCUMENT. Read each file before editing. Run `npx tsc --noEmit` and fix errors you introduced. Commit on `auto-2026-04-23` with message `[#<id>] <title>`. NEVER push. `git add <specific-paths>` not `-A`. No markdown plans. No TodoWrite. Report ≤200 words: commit hash, verify clean y/n, surprises, stubs.
4. Verify with `git show --stat <hash>`. Mark `- [x]` + hash.
5. After 2 failures mark `[FAILED] <reason>` and skip.
6. ScheduleWakeup(60s).

## Global context

- **Stack**: Next.js 15 + Firebase (App Hosting on `main`). Client in `src/`. Tailwind + shadcn. @radix-ui/react-collapsible is installed.
- **Dossier tabs infra** (existing): `src/hooks/use-dossier-tabs.ts` (provider + sessionStorage persistence at `sl-auto:dossier-tabs`), `src/components/layout/dossier-tabs-bar.tsx` (the bar). `openTab(dossierId, label)` and `closeTab(id)` are the API. `activeDossierId` derives from URL, is `null` when path isn't `/dossiers/[id]`.
- **Tabs bar visibility**: `src/app/(app)/layout.tsx:54` — `showDossierTabs && <DossierTabsBar />`. Currently true only on `/dossiers/[id]`. Task #1 needs it on `/dossiers` too.
- **DOC_SLOTS** (source of truth for piece-jointe types): `src/components/dossier-timeline/typed-documents-grid.tsx:34-46`. Includes `Facture Garage`, `Facture accordé`, `Devis Garage`, `Devis accordé`.
- **Firestore data model**:
  - `dossiers/{id}`: flat fields + `structuredEditables[docType]: StructuredDevis` (rows/header/versions[])
  - `dossiers/{id}/documents/{docId}`: piece-jointe records with `type`, `storagePath`, `url`, `nom`, `taille`
  - `dossiers/{id}/photos/{photoId}`: photo records with `category: 'avant' | 'en_cours' | 'apres'`
  - `chiffrages/{chiffrageId}`: mirror of structuredEditables
- **Logging helpers**: `src/app/(app)/dossiers/[id]/log-historique.ts` → `logHistorique(db, dossierId, action, user, details?, type?)` and `logWorkflow(db, dossierId, action, user, userId, status?, extra?)`. Grep for existing call sites to match format.
- **Comparer panel**: `src/app/editor/reference-panel.tsx` (mode='photos'|'documents', groups photos by category already). Uses `ZoomableImage` internal component at line 256.
- **Rapport PDF generators**: `src/lib/generate-rapport-preliminaire-pdf.ts`, `src/lib/generate-rapport-reforme-pdf.ts`, `src/lib/generate-rapport-shared.ts`.

## Locked assumptions

- **A1**: Chiffreur save for `docType === 'Facture Garage'` → upserts to `structuredEditables['Facture accordé']` on dossier + chiffrage, and upserts a single piece-jointe of type `'Facture accordé'` (find-by-type, update-or-create, never duplicate). Same for `Devis Garage` → `Devis accordé`. Garage doc stays untouched. `versions[]` appends each save (history lives there).
- **A2**: Dossier list page gets a non-closeable pinned-left pseudo-tab (label "Dossiers", sentinel id e.g. `__list__`). Clicking it → `router.push('/dossiers')`. `closeTab('__list__')` is a no-op. Active highlight = current path `/dossiers` and no dossierId.
- **A3**: Timeline step spacing = intrinsic. Replace `flex-1 h-px min-w-4` separators with `w-8 sm:w-12 h-px shrink-0`. Container keeps `overflow-x-auto` fallback for narrow viewports.
- **A4**: Comparer photos → three sub-tabs (Avant / En cours / Après) above a thumbnail grid (`grid-cols-3` at sm+, `grid-cols-2` mobile). Click thumbnail → takes the full panel via selected state + hide-grid-when-selected. Documents → rows stay, list vertical-expands (no `max-h-[160px]`); on select, auto-collapse list and viewer fills panel.
- **A5**: Rapport préliminaire PDF pulls `structuredEditables['Devis accordé']` + `structuredEditables['Facture accordé']` from the dossier. Rapport réforme PDF pulls `dossier.reforme`. Only render sections that have data (no stub tables for missing slots).
- **A6**: State persistence for collapsibles — `localStorage['pieces-{dossierId}-documents-open']` and `pieces-{dossierId}-photos-open`, boolean, default `true`.

## Tasks

- [x] **#1** — Dossier list as persistent pinned tab — `175afc7`
  - Files: `src/hooks/use-dossier-tabs.ts`, `src/components/layout/dossier-tabs-bar.tsx`, `src/app/(app)/layout.tsx`, `src/app/(app)/dossiers/client-page.tsx`
  - Extend `useDossierTabs`: add a derived "list tab" that is always present at index 0 (not stored in sessionStorage — always re-added on hydrate). Sentinel `dossierId: '__list__'`, label "Dossiers". `closeTab('__list__')` is no-op. `activeDossierId` stays as-is, but a new derived `activeTabId` returns `'__list__'` when pathname === `/dossiers`.
  - `DossierTabsBar`: render the `__list__` tab first with no X button; click → `router.push('/dossiers')`.
  - `layout.tsx`: expand `showDossierTabs` to include `/dossiers` exactly (not just `/dossiers/[id]`). Keep guard that hides bar on unrelated routes.
  - Done: open `/dossiers` → see "Dossiers" tab active, plus any previously-opened dossiers alongside; click a row → dossier tab opens; click "Dossiers" → back to list, dossier tab stays.

- [x] **#2** — Eye icon on source document in Step 1 Import — `62d66d0`
  - Files: `src/components/dossier-timeline/step-1-import.tsx`
  - When `dossier.importDocId` is set and the import doc is loaded, render an Eye icon button next to the filename. Click → open a preview Dialog (reuse pattern from `typed-documents-grid.tsx:79` with `previewDoc` state). Dialog shows iframe for PDFs, `<img>` for images. Reuse `isImage()` helper.
  - Done: Step 1 import section shows source doc with eye icon → click opens preview modal.

- [x] **#3** — Chiffreur save upserts to accordé slot — `9554152`
  - Files: `src/components/chiffreurs/devis-editor.tsx` (handleSave ~line 327-443), `src/lib/send-to-chiffrage.ts` (saveGestionnaireDevisAsPieceJointe ~148-288)
  - Add top-of-file helper `const ACCORDE_MAP: Record<string,string> = { 'Facture Garage': 'Facture accordé', 'Devis Garage': 'Devis accordé' }` and `const mapToAccorde = (t: string) => ACCORDE_MAP[t] ?? t`.
  - In **both** save paths, compute `targetDocType = mapToAccorde(docType)` and use it for the structuredEditables key, the piece-jointe `type`, the filename prefix, and the log message.
  - **Upsert piece-jointe**: before `addDoc`, query `collection(db, 'dossiers', dossierId, 'documents')` for `where('type', '==', targetDocType)` (or read the latest via existing subscription if simpler — just find the first match in memory), and if present, `updateDoc` that doc with new storagePath/url/taille/dateUpload; else `addDoc` as today.
  - Keep `versions[]` append semantics in `structuredEditables[targetDocType]` — that's the history.
  - Done: chiffreur edits + saves a Facture Garage → Facture accordé row appears once in pieces-jointes; second save overwrites storage + url, doesn't add a row; versions[] grows.

- [x] **#4** — Remove Exporter PDF topbar button — `69ef316`
  - Files: `src/app/(app)/dossiers/[id]/page.tsx` (lines ~156-168)
  - Remove that one Button JSX. Also remove `isExportingPdf` state + `setIsExportingPdf` + `generateRapportPDF` import if now unused anywhere in the file. Leave Envoyer vers chiffrage, Réclamation, Historique, Décision de statut alone.
  - Done: topbar no longer renders Exporter PDF; rapport step's Générer rapport still works.

- [x] **#5** — Dynamic timeline step spacing — `3fd35e5`
  - Files: `src/components/dossier-timeline/timeline-bar.tsx`
  - Line 20: remove `justify-between`; keep `flex items-center gap-2 overflow-x-auto`. Line 56: separator becomes `<div className="w-8 sm:w-12 h-px bg-border shrink-0" />`. Buttons already have `shrink-0` on the circle; ensure the label `<span>` doesn't steal width — it has `whitespace-nowrap` already.
  - Done: labels sit close together proportional to their content; no dead space; narrow viewports scroll horizontally.

- [x] **#6** — Collapsible Documents + Photos in pièces jointes — `cc774e1`
  - Files: `src/components/dossier-timeline/step-4-pieces.tsx`
  - Wrap each `<section>` in `<Collapsible>` (from `@/components/ui/collapsible`). Header becomes `<CollapsibleTrigger>` with a chevron that rotates on open. Body goes in `<CollapsibleContent>`.
  - Persist state with a small inline hook: `const [open, setOpen] = useState<boolean>(() => { try { const v = localStorage.getItem(\`pieces-\${dossierId}-documents-open\`); return v === null ? true : v === 'true'; } catch { return true; } });` plus `useEffect` to write on change. Same for photos.
  - Done: click headers → sections collapse/expand; refresh → state persists per dossier.

- [x] **#7** — Rapport PDFs read accordé + réforme data — `5c81e6f`
  - Files: `src/lib/generate-rapport-preliminaire-pdf.ts`, `src/lib/generate-rapport-reforme-pdf.ts`, maybe `src/lib/generate-rapport-shared.ts`
  - Read each file first. For preliminaire: locate the section where devis/facture should render; pull `dossierData.structuredEditables?.['Devis accordé']` + `['Facture accordé']` and render their `rows[]` into the PDF tables (designation, ref, qté, puHT, total). If a slot is missing, skip the section (don't render an empty table).
  - For reforme: confirm it reads `dossierData.reforme` and renders typeReforme, valeurVenale, valeurEpave, difference, totalIndemnisation. Fix gaps.
  - Done: generate a rapport préliminaire on a dossier with both accordé docs → PDF shows the line items. Generate a rapport réforme on a dossier with reforme data → PDF shows reforme fields.

- [x] **#8** — Remove chiffrage row clone button — `bc7f478`
  - Files: `src/components/chiffreurs/devis-editor.tsx`
  - Delete the `duplicateRow` function (~lines 286-291) and the Copy-icon button that calls it (~line 685). Remove now-unused `Copy` import from lucide-react if any. Leave addRow and deleteRow alone.
  - Done: row action buttons no longer include Dupliquer.

- [x] **#9** — Log entries on chiffrage save — `43a3120`
  - Files: `src/lib/send-to-chiffrage.ts` (after line 244 and after line 272), `src/components/chiffreurs/devis-editor.tsx` (after successful save ~line 443)
  - Import `logHistorique` + `logWorkflow` from `@/app/(app)/dossiers/[id]/log-historique`. After the dossier `updateDoc` write, call `logHistorique(db, dossierId, 'Enregistré ${targetDocType}', author.email, \`\${snapshot.rows.length} ligne(s) enregistrée(s)\`, 'document')`. After the chiffrage mirror, call `logWorkflow(db, dossierId, 'Devis/Facture mis à jour', author.email, author.uid, 'done', { details: targetDocType })`.
  - Same pattern in devis-editor handleSave for the chiffreur path.
  - Done: save a chiffrage from gestion → Historique sheet shows a new entry with author + doc type.

- [x] **#10** — Comparer photos sub-tabs + doc collapse-on-select — `a855649`
  - Files: `src/app/editor/reference-panel.tsx`
  - Photos mode: above the grouped list, add a sub-tab bar (Avant / En cours / Après) as a segmented control. Track `photoSubTab: 'avant' | 'en_cours' | 'apres'`. Render the **thumbnail grid** (each photo as a square tile with `object-cover`) for the active sub-tab — not filename rows. Remove the category header rows. When a thumbnail is clicked → `setSelectedId` AND `setListOpen(false)` so the viewer takes the full panel. Reuse `ZoomableImage` in the viewer.
  - Documents mode: lift the `max-h-[160px]` on the list container so it expands naturally. When a row is clicked → `setSelectedId` AND `setListOpen(false)`. The compact header (title "Comparaison" + chevron) stays visible so user can re-expand.
  - Done: Comparer → Photos → tab switching works, thumbnails, click-to-fullscreen works. Documents → list grows, click auto-collapses list and shows document.
